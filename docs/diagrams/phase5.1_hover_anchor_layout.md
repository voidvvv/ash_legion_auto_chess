# Phase 5.1 悬停预览卡锚点布局图（640×360 虚拟坐标，y 向上）

> 裁决 1 = A 固定锚悬停卡：棋盘悬停与商店卡悬停各定锚点。本图给出两锚点与 ①~⑨ 区的相对位置与防遮挡论证（计划 §5.3-1 的图形化）。
> 浏览器查看版：`phase5.1_hover_anchor_layout.html`（双击打开）
> 坐标口径：`BoardGeometry` 常量为 y 向上（左下原点），与 BattleRenderer 世界相机/Scene2d 一致；图按屏幕方向绘制（上= y 360）。

## 1. 布局示意图（约 20px/列，示意精度）

```
      x0         128       224       416       508   564  620  640
y360  +----------+---------+---------+---------+-----+----+----+
      | ① 顶栏（轮次/金币/等级+经验）              ①'暂停钮 x584 |
y342  +----------+---------+---------+---------+-----+----+----+
      | ⑧ 商店栏（5 卡 x12~480 · 刷新2金 x472 · 经验4金 x556） |
y296  +----------+---------+---------+---------+-----+----+----+
      |          |         ....引导文案....（基线 y280，居中）| ⑦ |
y280  | ⑨ 通知   |         :         :         :  K  : 出 |售 |
y244  | x20~148  | BOARD   | ④ 棋盘 6×7        | K   | 售区   |
      +----------+ HOVER   | x224~416          |（⑤下| x564   |
y192  | ③ 背包   | 卡位    | y50~274           | 空带）| ~620   |
      | x20~128  | BBBBBB  |                   +-----+----+----+
y172  +----------+ BBBBBB  |                   | ⑤ 羁绊面板     |
      | ② 备战席 | BBBBBB  |                   | SHOP HOVER     |
      | x20~128  | BBBBBB  |                   | 卡位覆盖此区    |
y128  | 3×3      | BBBBBB  |                   | （y48~240）    |
      |          | BB⑥BBB  |                   |                |
y88   |          | BBBBBB  |                   |                |
      +----------+---------+---------+---------+-----+----+----+
y48   +----------+---------+---------+---------+-----+----+----+
```

- `BOARD HOVER` = `BoardGeometry.BOARD_HOVER_X/Y/W/H = (128, 48, 94, 192)`，卡域 x 128~222、y 48~240。
- `SHOP HOVER` = `BoardGeometry.SHOP_HOVER_X/Y/W/H = (508, 48, 112, 192)`，卡域 x 508~620、y 48~240（与 ⑤ 羁绊面板同位）。
- `⑥` = 开战按钮（代码实际位 x134~198、y88~128，`ShoppingHud.java:34`；render §九 原表位 508,200 未用——差异声明 #2）。`K` = ⑤ 下方空带（y192~246，原 ⑥ 表位，空置）。

## 2. 逐带核对表（防遮挡论证数据）

| y 带（向上） | 内容 | BOARD_HOVER 关系 | SHOP_HOVER 关系 |
|------|------|------|------|
| 342~360 | ① 顶栏（Label x8 起 + 暂停钮 x584~632） | 无交集 | 无交集 |
| 296~360 | ⑧ 商店栏（全宽；悬停触发区） | 无交集（卡顶 240 < 296） | 无交集（指针在此区悬停才触发，卡不遮本区） |
| 274~296 | 备战引导文案带（基线 y280 居中） | 无交集 | 无交集 |
| 244~292 | ⑨ 通知（x20~148）＋ ⑦ 出售区（x564~620） | 卡底 240 < 244：**避开 ⑨/⑦** | 卡底 240 < 246：避开 ⑦ |
| 192~246 | ⑤ 下空带 K（x508~620，原 ⑥ 表位） | 无交集 | 无交集（卡底 240 略入此带 240~246 的 ⑤ 正下方空隙，不压 ⑦） |
| 48~192 | ⑤ 羁绊面板（x508~620） | 无交集（卡右缘 222 < 508） | **完全重合**（瞬态覆盖，移开即恢复） |
| 128~244 | ③ 背包（x20~128，y172~244） | 卡左缘 128 = ③ 右缘：相邻不压 | 无交集 |
| 48~168 | ② 备战席（x20~128） | 相邻不压；悬停源之一 | 无交集 |
| 50~274 | ④ 棋盘（x224~416） | 卡右缘 222 留 2px 间隙；悬停源之一 | 无交集 |
| 88~128 | ⑥ 开战按钮（x134~198，落左带内） | **瞬态覆盖**：指针移向按钮即离开棋子命中域 → 候选变 -1 → 卡立即隐藏，无点击阻塞（计划 §5.3-1） | 无交集 |
| 416~508 | 窄空带（全高，无归属） | 备选卡位（宽仅 86→6 列，否决） | 备选卡位（同前，否决） |

## 3. 备选方案存档（均被否决，理由见计划 §8 WARNING-5）

| 备选 | 矩形 | 否决理由 |
|------|------|------|
| BOARD 完全避让 ⑥ | x148~224 × y128~290（76×162） | 行容量 15→12、列宽 7→6；且避让收益为零（现方案本就无点击阻塞） |
| SHOP 避让 ⑤ | x420~506 × 高 244（86 宽） | 列宽 8→6、截断加剧；⑤ 为只读面板，瞬态覆盖代价低 |
| 顶部 hint 带放卡 | y274~296 全宽 | 带高仅 22px，容不下任何卡 |
