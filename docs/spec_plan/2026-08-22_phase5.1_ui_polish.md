# Phase 5.1 UI 补全（悬停预览 / 详情补全 / 全 UI 中文化）技术实施文档

> 版本：V1.0（2026-08-22）　分支：feature/phase_5　基线：42d9104（564 测试全绿；feedback01/02 修复与 #2 详情弹窗布局直修已入库）
> 输入：`docs/spec_plan/2026-08-22_phase5.1_ui_polish_brief.md`（feedback03 五项需求 + 四条用户裁决，裁决已随任务提供 → 未走澄清门禁）
> 范围声明：不改 Phase 5 计划（`2026-08-22_phase5_economy_ui_integration.md`）已交付范围；本期为其 UI 层补全。

---

## 1. 背景与目标

Phase 5 交付了完整单局闭环（经济/装备/宝箱/羁绊面板/通知/详情弹窗），但手验（feedback03）暴露三类缺口：

1. **先查看再决策缺失**（R1）：棋盘域只有按下/拖拽/松手三态（`BoardInputProcessor.java:157-160` 的 `mouseMoved` 恒 `return false`），商店卡无悬停反馈——玩家买前无法预览模板信息。
2. **详情信息不全**（R2）：`UnitDetailDialog` 只有名/星/spend/属性三行 + 卸下按钮（`UnitDetailDialog.java:97-102`）。技能描述侧 `skills.json` 已有 `desc` 且 `SkillData.getDesc()` 已就位（`SkillData.java:34`；`JsonLoader.java:188` 已 `requireString` 解析）——简报「缺则补 accessor」项核实为**已存在，零改动**。羁绊描述侧 `synergies.json` 无 `desc` 字段（裁决 2：加字段）。
3. **UI 英文残留**（R3）：顶栏/商店按钮/开战/投降/出售区/引导文案/胜负横幅/终局面板/主菜单/装载屏仍为英文（全量清单见 §5.4）。

**成功标准**（简报 §3）：

1. 悬停可先查后买：棋盘域（棋盘 6×7 + 备战席 3×3）与商店卡各自悬停 ~250ms 出固定锚只读预览卡；拖拽中、模态期、非 SHOPPING 抑制。
2. 详情弹窗含技能名+desc、两条羁绊 desc+档位数值行，属性标签全中文，且无遮挡（以 42d9104 布局修复为新基线重排）。
3. 通知与全部 UI 文案中文且术语一致（术语表见 §2）。
4. 564+ 测试全绿零回归（gradle XML 核对，沿用 MEMORY 口径）。

### 明确不做

| 项 | 出处 |
|----|------|
| 悬停卡跟随鼠标 / 交互式 tooltip | 裁决 1 = A 固定锚 |
| synergy 级 desc 由数据自动生成 | 裁决 2 = B 手写文案；仅档位数值行由结构化数据生成（混合口径 §5.3-3） |
| 新增羁绊（暗夜/精灵/植物/独眼种族、Boss 职业登记羁绊） | data_schema §六 V1.3 风味标签设定；属玩法变更，超出 5.1 |
| 修改 data_schema_design.md / render_design.md 等设计文档 | 沿 Phase 5「仅计划内差异声明」惯例（§5.2 #1；Phase 6 回写建议见 §8 WARNING-8） |
| 双语（中/英）文案表 | 裁决 3 = 全 UI 统一中文；CJK 降级口径见 §5.3-6 |
| 通知三流架构调整 | notices / NotificationFormat 实测已全中文（§3），仅 CAST 行技能名中文化（CP15） |

---

## 2. 术语与约定（GDD/简报用语 ↔ 代码标识符 ↔ UI 文案）

### 2.1 核心术语对照（本期全 UI 中文化的唯一词源，执行时不得自造同义词）

| GDD/设计文档用语 | 代码标识符 | UI 文案（定稿） | 备注 |
|------|------|------|------|
| 生命值 | `StatKey.HP` / `BaseStats.getHp()` | 生命 | 属性行短形（简报 R2 映射） |
| 攻击力 | `StatKey.ATTACK` | 攻击 | 同上 |
| 护甲 | `StatKey.ARMOR` | 护甲 | 同上 |
| 攻击速度 | `StatKey.ATTACK_SPEED` | 攻速 | 同上 |
| 移动速度 | `StatKey.MOVE_SPEED` | 移速 | 同上 |
| 射程 | `StatKey.RANGE` | 射程 | 同上 |
| 吸血 | `StatKey.LIFESTEAL` | 吸血 | 百分点刻度，文案附 % |
| 能量获取 | `StatKey.ENERGY_GAIN_RATE` | 回能 | 同上 |
| 技能强度 | `StatKey.SKILL_POWER` | 技能强度 | 同上 |
| 累计花费 | `Unit.spend` / `getSpend()` | 累计花费 | GDD §3.6 |
| 费阶 | `UnitData.cost` | N费（如 1费） | |
| 星级 | `Unit.star` | N星 | |
| 轮次 | `RunState.round` | 轮次 X/25 | |
| 金币 | `Player.gold` | 金币 | |
| 等级 / 经验 | `Player.level` / `currentExp` | 等级 / 经验；Lv.7 封顶 → 满级 | |
| 羁绊 / 种族 / 职业 | `SynergyData` / `race` / `unitClass` | 羁绊 / 种族（RACE）/ 职业（CLASS） | |
| 商店刷新 | `RefreshShopCommand`（2 金） | 刷新 2金 | |
| 购买经验 | `BuyExpCommand`（4 金） | 经验 4金 | |
| 开战 | `StartBattleCommand` | 开战 | |
| 投降 / 放弃远征 | `SurrenderCommand` / `AbandonRunCommand` | 投降 / 放弃远征 | |
| 变速 | `speedFactor`（×1/×2） | 速度 x1 / 速度 x2 | 只改表演速率，不进模拟路径 |
| 出售区 | `BoardGeometry.SELL_ZONE_*` | 出售 | |
| 胜负 | `BattleOutcome` | 胜利 / 战败 / 超时 | |
| 宝箱领取提示 | — | 选择一个宝箱 | RESULT 胜局唯一出口 |
| 败局继续提示 | — | 点击任意处重试 | 1C-R 原地重试 |
| 终局成因 | `RunEndCause` | 远征通关 / 远征已放弃 | |
| 重开 | `restartRun` | 重新开始 | |
| 游戏名 | 项目名 ember-legion | 余烬军团 | game_lore §标题（暂定名） |

### 2.2 本期新概念

| 简报用语 | 代码标识符 | 位置 |
|------|------|------|
| 悬停预览卡（棋盘域） | `render/ui/HoverPreviewCard` + `BoardGeometry.BOARD_HOVER_*` | CP8 |
| 悬停预览卡（商店卡锚） | `BoardGeometry.SHOP_HOVER_*` | CP8 |
| 悬停状态机（250ms 驻留） | `render/ui/HoverStateMachine` | CP5 |
| 悬停候选（棋盘域） | `BoardInputProcessor.getHoverCandidateUnitId()` | CP6 |
| 悬停槽位（商店） | `ShopBar.getHoveredSlot()` | CP7 |
| 文案格式化纯函数 | `render/ui/UnitInfoText` | CP1 |
| 羁绊描述字段 | `SynergyData.desc` / synergies.json `desc` | CP2~CP4 |
| 档位数值行（生成式） | `UnitInfoText.synergyTierLine(SynergyData)` | CP1 |

---

## 3. 现状盘点（file:line 均为本次实读核对，基线 42d9104）

### 3.1 可直接复用（零改动）

| 资产 | 位置 | 说明 |
|------|------|------|
| 技能 desc 数据与 accessor | `assets/data/skills.json`（11 条全有 desc）；`SkillData.getDesc()`（SkillData.java:34）；`JsonLoader.parseSkills` requireString（JsonLoader.java:188） | 简报 R2「核对是否已解析」结论：**已就位** |
| 通知系统反应行 | `ShopSystem.java:111,148,176`、`RosterSystem.java:39`、`EquipmentSystem.java:35,52`、`RunFlowSystem.java:97,124,197,226`、`ChestSystem.apply` | **实测已全中文**（简报 R3 该项为过时描述，§5.2 #5） |
| 通知命令静态行 | `NotificationFormat.formatCommand`（NotificationFormat.java:17-28） | 已中文 |
| 已中文的 UI | `PauseMenuDialog`（继续/放弃远征/确认放弃/取消/暂停）、`ChestDialog`（金币/经验/选择/宝箱）、`InventoryPanel`（武/甲/饰/+N）、`SynergyPanel`（羁绊/-）、`EquipPendingState`（无文案） | 零改动 |
| 模态阻断位 | `BoardInputProcessor.java:53`（`BooleanSupplier modalBlocked`，BattleScreen.java:209-214 已接 `dialogManager::isShowing`） | CP6 悬停抑制直接复用 |
| 名单按 id 定位 | `Player.getUnitById`（Phase 5 CP6 交付） | 悬停候选名单核验、弹窗过期判定共用 |
| 商店槽位查询 | `RunContext.getShop().slotAt(i)`（ShopBar.java:81 先例） | 商店悬停解析模板 |
| 渲染帧驱动点 | `BattleScreen.render`（BattleScreen.java:238-283） | CP16 悬停卡 refresh 挂接点 |
| 测试基建 | `BoardInputProcessorTest`（IdentityViewport 桩，:43-48）、`UnitDetailDialogTest`、`TopBarTextTest`、`JsonLoaderTest`（真种子 ../assets/data）、`JsonLoaderValidationTest`（@TempDir 内联 JSON） | 各 CP 测试沿用先例 |

### 3.2 需改造

| 文件 | 位置 | 改动 |
|------|------|------|
| `data/SynergyData.java` | :15-30 构造与字段 | 增 `desc` 字段 + 兼容重载 + getter（CP2） |
| `config/JsonLoader.java` | :261（checkUnknownKeys）、:263（name 后）、:298（构造调用） | 解析必填 `desc`（CP3） |
| `assets/data/synergies.json` | 全文（6 条） | 每条加 `desc` 手写文案（CP4） |
| `input/BoardInputProcessor.java` | :157-160（mouseMoved）、:78-94（touchDown）、字段段 | 悬停轨迹 + 候选查询（CP6） |
| `render/ui/ShopBar.java` | :30-67（构造）、:104-136（ShopCard） | 悬停槽位 + 按钮中文化（CP7） |
| `render/board/BoardGeometry.java` | :51 之后 | 悬停卡锚点常量 ×8（CP8） |
| `render/ui/UnitDetailDialog.java` | 全文（152 行） | 信息架构重排 + 中文化（CP9） |
| `render/ui/TopBar.java` | :53-60（statusText） | 中文化（CP10） |
| `render/ui/BattleHud.java` | :76-85、:117-123 | 速度/投降 中文化（CP11） |
| `render/ui/ShoppingHud.java` | :43-50 | 开战 中文化（CP11） |
| `render/board/BattleRenderer.java` | :37-38、:201-208、:210-217 | 出售/引导文案 + 过时注释（CP12） |
| `render/ui/ResultBanner.java` | :49-64、:66-79 | 胜负文案与定位（CP13） |
| `render/ui/RunEndPanel.java` | :42-56、:69-76 | 终局文案与定位（CP13） |
| `screens/MainMenuScreen.java` | :87-94 | 开始远征（CP14） |
| `screens/LoadingScreen.java` | :13-17（注释）、:46-47 | 余烬军团/装载中（CP14） |
| `render/ui/NotificationPanel.java` | :67-75（调用）、:91-100（formatEvent） | CAST 行技能名中文（CP15） |
| `screens/BattleScreen.java` | :89-96（字段）、:140-179（构造）、:261-262（render） | 悬停卡装配（CP16） |
| 测试改写 | `TopBarTextTest`、`JsonLoaderValidationTest`（VALID_SYNERGY）、`JsonLoaderTest`（desc 断言）、`NotificationFormatTest`（CAST 行） | 随对应 CP |

### 3.3 需新建

| 文件 | CP |
|------|----|
| `core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/UnitInfoText.java`（文案格式化纯函数） | CP1 |
| `core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/HoverStateMachine.java`（250ms 驻留状态机） | CP5 |
| `core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/HoverPreviewCard.java`（悬停预览卡 Actor） | CP8 |
| 测试：`UnitInfoTextTest`、`HoverStateMachineTest`；`BoardInputProcessorTest`/`BoardGeometryTest` 增例 | CP1/CP5/CP6/CP8 |

### 3.4 事实核对结论（与简报/任务描述的差异）

| # | 简报/任务描述 | 实测 | 处置 |
|---|------|------|------|
| 1 | 简报 R3「systems 内 addNotice 的英文串」 | 全部已中文（§3.1） | 无需改 systems；WARNING-7 存档 |
| 2 | 简报 R3「BattleHud 投降（SURRENDER）」 | 实际按钮文案为 `GIVE UP`（BattleHud.java:122） | CP11 改「投降」 |
| 3 | 任务描述「12 条羁绊手写文案」 | `synergies.json` 实际 **6 条**（syn_orc/warrior/mage/assassin/beast/ranger；`JsonLoaderTest.java:42` 断言 hasSize(6)）；暗夜/精灵/植物/独眼与 Boss 为风味标签，刻意无羁绊（data_schema §六 V1.3） | desc 按 6 条写全；WARNING-1 存档（若需扩羁绊属玩法变更，另立内容任务） |
| 4 | 简报 R2「核对 SkillData 是否已解析 desc，缺则补 accessor」 | 已解析+已有 accessor | 零改动 |

---

## 4. 已确认决策（用户裁决，2026-08-22，最终决定，不得改回）

| # | 议题 | 裁决 | 本文档落地 |
|---|------|------|------|
| 1 | 悬停预览交互方案 | **A 固定锚悬停卡**：悬停 ~250ms 显示、不跟随鼠标、固定卡位（棋盘悬停与商店卡悬停各定锚点）、只读精简、拖拽中与模态期抑制、与详情弹窗共用格式化函数 | CP1（共用 `UnitInfoText`）/CP5（250ms）/CP6+CP7（双源）/CP8（锚点与防遮挡论证 §5.3-1） |
| 2 | 羁绊描述来源 | **B synergies.json 加 desc 字段**（结构锁定变更已获批，手写文案） | CP2~CP4；混合口径定稿见 §5.3-3：synergy 级 `desc` 一句主题描述手写 + 档位数值行由 thresholds/effects 生成 |
| 3 | 中文化范围 | **A 全 UI 统一**（术语按 GDD） | 术语表 §2.1；全量英文清单与改写 §5.4；CP7/CP9~CP15 |
| 4 | 执行链 | 简报 → planner 详细计划 → executor；#2 布局缺陷已直修 | 本文档即 planner 产出；详情弹窗以 42d9104 为基线重排（CP9） |

---

## 5. 总体技术方案

### 5.1 架构与数据流

分层不动（data/entities → systems → command → render/input → screens）。本期全部改动位于 data（desc 字段）与 render/input/screens（UI），**零新增命令、零 RNG 消耗点、零模拟路径改动**。

悬停链路（裁决 1 的落地形态）：

- **棋盘域源**：`BoardInputProcessor.mouseMoved`（新增，CP6）记录 `hoverUnitId`（经 `unitAt` 命中：备战槽/玩家区格）；查询侧 `getHoverCandidateUnitId()` 集中施加抑制（拖拽中 `isDragging()` / `modalBlocked` / 非 SHOPPING / 名单核验 `getUnitById`）。`mouseMoved` 返回 `false` 不消费事件。
- **商店源**：`ShopCard` 增 Scene2D `InputListener` enter/exit（CP7）维护 `ShopBar.hoveredSlot`，`getHoveredSlot()` 暴露；空槽（模板 null）在 `HoverPreviewCard` 侧归一为无候选。
- **计时**：`HoverStateMachine`（CP5，纯逻辑）×2 实例（棋盘/商店各一，双源互斥——单指针不同时悬停两处，棋盘源优先）。候选变化重置计时；候选为空或 suppressed（= BattleScreen `frozen`：paused || 弹窗模态）立即清零隐藏。
- **渲染**：`HoverPreviewCard`（CP8，uiStage 最上层 Actor，非模态瞬态）每帧由 `BattleScreen.render` 显式 `refresh(候选, 槽位, frozen, delta)`（CP16）；行集 = `UnitInfoText.previewLines(模板, GameData, maxColumns)`（CP1，纯函数 headless 可测），超卡高按 §5.3-4 截断。
- **详情弹窗**（CP9）与悬停卡共用 `UnitInfoText`（裁决 1「共用格式化函数」；弹窗用实例级 `detailLines`，悬停卡用模板级 `previewLines`）。

图表（落盘 `docs/diagrams/`，本文档只放引用）：

| 图 | 覆盖 | 文件 |
|----|------|------|
| 悬停状态机 + 预览数据流 | 双源→状态机→卡→UnitInfoText→GameData(desc) | `docs/diagrams/phase5.1_hover_flow.md`（+ .html） |
| 悬停卡锚点布局（640×360 九区防遮挡） | BOARD_HOVER / SHOP_HOVER 与 ①~⑨ 相对位置 | `docs/diagrams/phase5.1_hover_anchor_layout.md`（+ .html） |

### 5.2 与设计文档的差异声明（如实记录，不改设计文档）

| # | 设计文档条款 | 本期实现 | 依据 |
|---|------|------|------|
| 1 | data_schema §六 synergies 字段表无 `desc` | 增 synergy 级必填 `desc`（string，加载期 fail-fast，与 skills.json desc 同口径）；文档回写推 Phase 6（§8 WARNING-8） | 裁决 2（结构锁定变更已批） |
| 2 | render §九 ⑥ 开战按钮 (508,200,112,40) | 代码实际位于 (134,88,64,40)（ShoppingHud.java:34，Phase 4 布局既成偏离，本期不动）；因此 ⑤ 下方 y 192~244 一带实际空置，SHOP_HOVER 锚点覆盖 ⑤ 有论证基础（§5.3-1） | 现状 |
| 3 | user_input §2.4 死区点击「Tooltip / 选中态」 | 以「固定锚悬停卡（只读预览）+ 点击详情弹窗（可操作）」两态落地 | 裁决 1 |
| 4 | render §十一 待定清单（Tooltip） | 棋盘域 + 商店卡悬停预览本期落地（待定项销账） | 裁决 1 |
| 5 | 简报 R3「notices 动态行英文」 | 实测已全中文，systems 零改动 | §3.4 #1 |
| 6 | LoadingScreen 既有注释「本期屏显文案用 ASCII（默认字体无 CJK 字模）」 | Fusion Pixel 已入库（assets/font/），装载屏改中文；该注释过时一并修正 | 裁决 3 + 现状 |

### 5.3 实现层口径（文档未明说、本次定的执行细节——评审重点）

**1. 悬停卡锚点坐标与防遮挡论证**（左上角原点，虚拟 640×360）：

| 锚点 | 常量（BoardGeometry，CP8） | 防遮挡论证 |
|------|------|------|
| 棋盘域悬停卡 | `BOARD_HOVER_X=128, Y=48, W=94, H=192`（卡域 x 128~222，y 48~240） | ① 占用 ②③（右边界 x=128）与 ④ 棋盘（左边界 x=224）之间的左侧空带，右缘 222 留 2px 间隙；② 下界避开 ⑨ 通知面板（NOTIFY_Y=244 起，卡底 240 留 4px）；③ 上界沿 ② 备战席顶 48；④ 不压 ④ 棋盘与 ②③（拖拽 ghost、落点高亮、备战席点击均在卡域之外）。**已知瞬态覆盖 ⑥ 开战按钮**（代码定位 x134~198, y88~128，ShoppingHud.java:34，落在本带内）：不构成点击阻塞——指针自棋子移向开战钮必然先离开棋子命中域，`hoverUnitId` 即变 -1、卡**立即**隐藏（候选变化清显示无 250ms 延迟），到达按钮时卡已不在；悬停查看与点击开战亦为互斥意图。备选「完全避让 ⑥」矩形 x148~224 × y128~290（76×162）因行容量降至 12、列宽降至 6 被否决（§8 WARNING-5） |
| 商店卡悬停卡 | `SHOP_HOVER_X=508, Y=48, W=112, H=192`（卡域 x 508~620，y 48~240） | 精确覆盖 ⑤ 羁绊面板区。理由：(a) 悬停商店卡时指针在 y≥296 的 ⑧ 区，卡不遮挡棋盘/备战席/商店栏/刷新与经验按钮；(b) ⑤ 是只读面板且瞬态覆盖，移开鼠标即恢复；(c) 该区域右侧空带（render §九 ⑥ 原表位）实际已被代码挪至左侧（差异声明 #2），x 508~620 是右侧唯一能容纳 112 宽卡位的整块区域；(d) 下界 240 避开 ⑦ 出售区（y 246 起） |

两个锚共用一个 `HoverPreviewCard` 组件与同一行集函数，仅卡位与 `maxColumns` 不同（棋盘 7 列 / 商店 8 列，见口径 5）。

**2. 250ms 计时器实现**：`HoverStateMachine`（CP5）为 accumulator 型（帧 `delta` 累加至 `DELAY_SECONDS=0.25f` 显示），非逻辑钟步进——悬停是纯表现层行为，不进 `stepSimulation` 固定步长（architecture §4.2 暂停=表演层的同类口径）；`BattleScreen.render` 以 `frozen ? 0f : delta` 喂入（模态/暂停期计时不推进且立即隐藏）。候选 id 变化即重置计时（同格内微移不重抖）。

**3. 羁绊 desc 混合口径（定稿）**：synergy 级 `desc` = 一句手写主题描述（lore 对齐，CP4）；档位数值行 = `UnitInfoText.synergyTierLine` 由 `thresholds[].count + effects[]{stat,op,value}` 结构化生成，格式 `2:生命+150；4:生命+400·攻击+20%；6:护盾30%·吸血+20`（档间 `；`、同档效果间 `·`；ADD→`+v`（百分点刻度键附 `%`）、PCT→`+v%`、SHIELD→`护盾NN%`）。数值改表即改文案，手写文案只管主题——两全（简报 R2 建议采纳）。

**4. 悬停卡截断策略**：卡高 192 → 行容量 `floor((192-8)/12)=15` 行（12px 字体、行距 12、顶留 4）。行集按信息优先序排列（名/费阶 → 属性 3 行 → 技能名 → 技能 desc → 羁绊块[名+desc+档位行]×≤2）；超出容量的行丢弃，末行以 `…` 示意（`UnitInfoText.clipLines`，纯函数）。最坏情形（双羁绊+长 desc，约 20~28 行）必然截断——裁决 1「只读精简」授权；完整档位数值走点击详情弹窗（弹窗 380px 宽不折行，全量展示）。若手验发现关键信息常被截，调整项见 §8 WARNING-2。

**5. 折行算法**：`UnitInfoText.wrap(text, maxColumns)`，列宽口径：全角字符（CJK/全角标点，>`0xFF`）= 1 列、半角 = 0.5 列（Fusion Pixel 12px 实测：CJK 12px、ASCII 约 6px）；空格断点贪心折行、单 token 超行宽按字符硬断。棋盘卡 `maxColumns=7`（(94-8)/12≈7.2），商店卡 `maxColumns=8`（(112-8)/12≈8.7）。纯函数 headless 可测（不依赖 BitmapFont/GL）。

**6. CJK 降级口径（定稿，二选一取「乱码可接受」）**：`Assets.font()` 缺 `font/fusion_pixel_12.fnt` 回退内置默认字体（Assets.java:45-51 既有分支）时无 CJK 字模——中文字符不渲染（libGDX BitmapFont 对缺字模字符跳过，显示空白，**不抛异常**）。接受该降级表现，理由：(a) 正式构建 Fusion Pixel 已入库，回退仅为 headless 测试/素材缺失异常路径；(b) 维护中英双语句表会使全部文案 CP 翻倍并引入术语漂移风险，违背裁决 3「术语统一」的初衷。数值类（金币/轮次数字）在降级态仍可读，核心玩法不失效。

**7. ShopBar 宽度适配（英文→中文）**：`REFRESH 2G`（10 ASCII ≈ 60px）→ `刷新 2金`（≈48px）、`EXP 4G` → `经验 4金`（≈48px），均窄于原文；按钮 80px 宽不变，绘字偏移 +8 → +16 居中。卡面费用 `1G` → `1金`（≈18px，卡宽 84 内富余）。顶栏中文后约 180px 宽（x=8 起，pause 钮在 x=584），无碰撞。

**8. 悬停抑制条件汇总**（集中两处，避免散落）：棋盘源在 `BoardInputProcessor.getHoverCandidateUnitId()`（拖拽/模态/非 SHOPPING/名单失效）；商店源在 `ShopBar.getHoveredSlot()` 原始暴露 + `HoverPreviewCard.refresh` 归一（非 SHOPPING / 空槽 / suppressed）。两源共用 `frozen` 抑制位。

**9. desc 必填与既有口径类推**：skills.json `desc` 为 `requireString` 必填（JsonLoader.java:188，缺即 `DataValidationException`）——synergies.json `desc` 同口径必填致命（非软告警）。理由：结构锁定已获批（裁决 2）、手写文案漏写应在加载期暴露而非运行期静默缺行。测试夹具影响有界（§3.2 测试改写清单）。

**10. `SynergyData` 兼容重载**：6 参 canonical 构造（含 desc）+ 5 参兼容重载（desc=""，委托）——沿 `Unit`（Phase 5 CP5）3→4 参先例，7 处测试直接构造点（SynergyDataTest×2、BattleSystemTest、SynergySystemTest×2、BattleTestFixtures、SynergyPanelTest×2 中的 5 参调用）零改动；空 desc 由 `UnitInfoText` 跳过 desc 行（防御）。

### 5.4 全量英文出没点复核清单（grep 全量核对，R3 执行表）

| # | 位置 | 现文案（逐字） | 改为 | CP |
|---|------|------|------|----|
| 1 | TopBar.java:56-59 | `ROUND x/25  GOLD g  LV l (e/n)` / `MAX` | `轮次 x/25  金币 g  等级 l（e/n）` / `满级` | CP10 |
| 2 | TopBar.java:80 | `II`（暂停符号） | 保留（符号非英文词） | — |
| 3 | BattleHud.java:83 | `SPD x1` / `SPD x2` | `速度 x1` / `速度 x2` | CP11 |
| 4 | BattleHud.java:122 | `GIVE UP` | `投降` | CP11 |
| 5 | ShoppingHud.java:49 | `FIGHT` | `开战` | CP11 |
| 6 | ShopBar.java:51 | `REFRESH 2G` | `刷新 2金` | CP7 |
| 7 | ShopBar.java:59 | `EXP 4G` | `经验 4金` | CP7 |
| 8 | ShopBar.java:133 | `NG`（费用） | `N金` | CP7 |
| 9 | UnitDetailDialog.java:97-102 | `spend` / `HP ATK ARMOR` / `ASPD RANGE MSPD` | 中文标签（§2.1）+ 技能/羁绊行 | CP9 |
| 10 | UnitDetailDialog.java:123 | `X`（关闭） | `关闭` | CP9 |
| 11 | BattleRenderer.java:38 | `DRAG UNITS TO BOARD · DRAG TO SELL · THEN FIGHT` | `拖拽棋子布阵 · 拖至右下出售区卖出 · 点开战` | CP12 |
| 12 | BattleRenderer.java:207 | `SELL` | `出售` | CP12 |
| 13 | ResultBanner.java:52/54/58/60 | `VICTORY`/`pick a chest`/`DEFEAT`/`TIMEOUT`/`click to retry` | `胜利`/`选择一个宝箱`/`战败`/`超时`/`点击任意处重试` | CP13 |
| 14 | RunEndPanel.java:50-55/75 | `RUN ABANDONED`/`RUN COMPLETE`/`survived to round…`/`mastery +N (stub, Phase 6)`/`seed N`/`RESTART` | `远征已放弃`/`远征通关`/`抵达第 X/25 轮`/`熟练度 +N（Phase 6 接档案）`/`种子 N`/`重新开始` | CP13 |
| 15 | MainMenuScreen.java:93 | `START` | `开始远征` | CP14 |
| 16 | LoadingScreen.java:46-47 | `EMBER LEGION` / `loading...` | `余烬军团` / `装载中……` | CP14 |
| 17 | NotificationPanel.java:96 | `技能施放: skill_xxx`（英文 id） | `技能施放：<中文名>`（GameData 查表，失败回退 id） | CP15 |
| 18 | PlaceholderKeys/RealArt/DigitGlyph 等 | 键名/数字 | 保留（非玩家可见文案） | — |

---

## 6. 改动点清单（评审主入口）

全部改动逐一列出，编号全局唯一（CP1~CP16），按依赖顺序排列。**去重规则**：同一段代码的完整改动只在一个 CP 出现，其余改动点一律章节引用。

---

### CP1. UnitInfoText 文案格式化纯函数（悬停卡/详情弹窗共用）

- **类型**：新建文件
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/UnitInfoText.java`
- **改动说明**：裁决 1「与详情弹窗共用格式化函数」的落地。纯静态函数、零 Gdx 依赖（headless 可测，TDD 先行）。职责：属性短名词表（§2.1）、档位效果数值文案、羁绊档位行（§5.3-3 混合口径）、按 source+key 查羁绊（风味种族/职业返回 null，不显示——data_schema §六 V1.3）、悬停预览行集（模板级，不含 spend/已穿装备，简报 R1）、详情行集（实例级：星/spend）、折行（§5.3-5 列宽口径）、卡高截断（§5.3-4）。GDD 用语 ↔ 标识符对照见 §2.1/§2.2。
- **代码**（完整新建）：

```java
package com.voidvvv.kz_auto_chess_n.render.ui;

import com.voidvvv.kz_auto_chess_n.data.BaseStats;
import com.voidvvv.kz_auto_chess_n.data.EffectData;
import com.voidvvv.kz_auto_chess_n.data.EffectOp;
import com.voidvvv.kz_auto_chess_n.data.GameData;
import com.voidvvv.kz_auto_chess_n.data.SkillData;
import com.voidvvv.kz_auto_chess_n.data.StatKey;
import com.voidvvv.kz_auto_chess_n.data.StatusType;
import com.voidvvv.kz_auto_chess_n.data.SynergyData;
import com.voidvvv.kz_auto_chess_n.data.SynergySource;
import com.voidvvv.kz_auto_chess_n.data.UnitData;
import com.voidvvv.kz_auto_chess_n.entities.Unit;

import java.util.ArrayList;
import java.util.List;

/**
 * 棋子文案格式化纯函数（Phase 5.1 R1/R2）：悬停预览卡（棋盘域 + 商店卡）与详情弹窗
 * 共用的行集拼装——零 Gdx 依赖，headless 可测（TDD 先行）。
 *
 * <p>术语表见计划 §2.1（属性短名与 GDD 全称一一对应）。档位数值行由
 * thresholds/effects 结构化数据生成（R2 混合口径：synergy 级 desc 手写 + 档位行生成）。
 * 折行列宽口径：全角（含 CJK）= 1 列、半角 = 0.5 列（Fusion Pixel 12px 实测）。
 */
public final class UnitInfoText {

    private UnitInfoText() {
    }

    // —— 词表（§2.1 属性行短形；档位行共用） ——

    /** 属性短名（悬停卡/详情弹窗属性行与档位行共用） */
    public static String statLabel(StatKey key) {
        switch (key) {
            case HP: return "生命";
            case ATTACK: return "攻击";
            case ARMOR: return "护甲";
            case ATTACK_SPEED: return "攻速";
            case MOVE_SPEED: return "移速";
            case RANGE: return "射程";
            case LIFESTEAL: return "吸血";
            case ENERGY_GAIN_RATE: return "回能";
            case SKILL_POWER:
            default: return "技能强度";
        }
    }

    /** 单条档位效果 → 数值文案：ADD → +v（百分比刻度键附 %）；PCT → +v%；SHIELD → 护盾NN% */
    static String effectText(EffectData effect) {
        if (!effect.isStatChannel()) {
            if (effect.getEffect() == StatusType.SHIELD) {
                return "护盾" + Math.round(effect.getValue() * 100f) + "%";
            }
            return effect.getEffect().jsonName(); // 未知特殊效果：保守回退词表名（不炸）
        }
        String label = statLabel(effect.getStat());
        if (effect.getOp() == EffectOp.PCT) {
            return label + "+" + numberText(effect.getValue()) + "%";
        }
        String suffix = effect.getStat().isPercentScale() ? "%" : "";
        return label + "+" + numberText(effect.getValue()) + suffix;
    }

    /** 羁绊档位行（结构化生成，R2 混合口径）：如 2:生命+150；4:生命+400·攻击+20% */
    public static String synergyTierLine(SynergyData synergy) {
        StringBuilder sb = new StringBuilder();
        for (SynergyData.Threshold tier : synergy.getThresholds()) {
            if (sb.length() > 0) {
                sb.append("；");
            }
            sb.append(tier.getCount()).append(":");
            for (int i = 0; i < tier.getEffects().size(); i++) {
                if (i > 0) {
                    sb.append("·");
                }
                sb.append(effectText(tier.getEffects().get(i)));
            }
        }
        return sb.toString();
    }

    /** 按 source+key 找羁绊（unit race/class → 登记表）；未登记返回 null（风味标签，§六 V1.3） */
    public static SynergyData findSynergy(GameData data, SynergySource source, String key) {
        for (SynergyData synergy : data.getSynergies().values()) {
            if (synergy.getSource() == source && synergy.getKey().equals(key)) {
                return synergy;
            }
        }
        return null;
    }

    // —— 悬停预览行集（棋盘域 + 商店卡共用；模板级，不含 spend/已穿装备——R1） ——

    /**
     * 预览行集（按信息优先序）：名/费阶 → 属性 3 行 → 技能名 → 技能 desc →
     * 羁绊块（名 + desc + 档位行）×≤2（未登记的种族/职业不显示）。
     *
     * @param maxColumns 行宽上限（列；≤0 = 不折行）
     */
    public static List<String> previewLines(UnitData template, GameData data, int maxColumns) {
        List<String> lines = new ArrayList<String>();
        BaseStats stats = template.getBaseStats();
        addWrapped(lines, template.getName() + " " + template.getCost() + "费", maxColumns);
        addWrapped(lines, statPair("生命", stats.getHp(), "攻击", stats.getAttack()), maxColumns);
        addWrapped(lines, statPair("护甲", stats.getArmor(), "攻速", stats.getAttackSpeed()), maxColumns);
        addWrapped(lines, statPair("射程", stats.getRange(), "移速", stats.getMoveSpeed()), maxColumns);
        SkillData skill = data.getSkill(template.getSkillId());
        if (skill != null) {
            addWrapped(lines, "技能 " + skill.getName(), maxColumns);
            addWrapped(lines, skill.getDesc(), maxColumns);
        }
        appendSynergyPreview(lines, findSynergy(data, SynergySource.RACE, template.getRace()), maxColumns);
        appendSynergyPreview(lines, findSynergy(data, SynergySource.CLASS, template.getUnitClass()), maxColumns);
        return lines;
    }

    private static void appendSynergyPreview(List<String> lines, SynergyData synergy, int maxColumns) {
        if (synergy == null) {
            return;
        }
        addWrapped(lines, "羁绊 " + synergy.getName(), maxColumns);
        addWrapped(lines, descOrEmpty(synergy), maxColumns);
        addWrapped(lines, synergyTierLine(synergy), maxColumns);
    }

    // —— 详情弹窗行集（实例级：星/spend + 属性 + 技能 + 两条羁绊；装备区由弹窗按钮行承担） ——

    public static List<String> detailLines(Unit unit, GameData data) {
        List<String> lines = new ArrayList<String>();
        UnitData template = unit.getTemplate();
        BaseStats stats = template.getBaseStats();
        lines.add(template.getName() + " " + unit.getStar() + "星（" + template.getCost()
                + "费）· 累计花费 " + unit.getSpend());
        lines.add("生命 " + numberText(stats.getHp()) + "   攻击 " + numberText(stats.getAttack())
                + "   护甲 " + numberText(stats.getArmor()));
        lines.add("攻速 " + numberText(stats.getAttackSpeed()) + "   射程 " + numberText(stats.getRange())
                + "   移速 " + numberText(stats.getMoveSpeed()));
        SkillData skill = data.getSkill(template.getSkillId());
        if (skill != null) {
            lines.add("技能 " + skill.getName() + "：" + skill.getDesc());
        }
        appendSynergyDetail(lines, findSynergy(data, SynergySource.RACE, template.getRace()));
        appendSynergyDetail(lines, findSynergy(data, SynergySource.CLASS, template.getUnitClass()));
        return lines;
    }

    private static void appendSynergyDetail(List<String> lines, SynergyData synergy) {
        if (synergy == null) {
            return;
        }
        String desc = descOrEmpty(synergy);
        lines.add("羁绊 " + synergy.getName() + (desc.isEmpty() ? "" : "：" + desc));
        lines.add(synergyTierLine(synergy));
    }

    // —— 数值与折行（纯函数，headless 可测） ——

    /** 数值显示：整数去小数尾（1.0 → 1、20 → 20），非整数保留（0.3 → 0.3、1.5 → 1.5） */
    static String numberText(float value) {
        float rounded = Math.round(value * 100f) / 100f; // 浮点尾差收口（0.3f*100 → 30.000001 → 0.3）
        if (rounded == Math.floor(rounded)) {
            return String.valueOf((long) rounded);
        }
        return String.valueOf(rounded);
    }

    private static String statPair(String labelA, float valueA, String labelB, float valueB) {
        return labelA + numberText(valueA) + " " + labelB + numberText(valueB);
    }

    private static String descOrEmpty(SynergyData synergy) {
        String desc = synergy.getDesc();
        return desc == null ? "" : desc;
    }

    private static void addWrapped(List<String> lines, String text, int maxColumns) {
        if (text != null && !text.isEmpty()) {
            lines.addAll(wrap(text, maxColumns));
        }
    }

    /** 空格断点贪心折行；单 token 超行宽按字符硬断；maxColumns ≤ 0 或整行已容纳 = 原样单行 */
    public static List<String> wrap(String text, int maxColumns) {
        List<String> result = new ArrayList<String>();
        if (text == null || text.isEmpty()) {
            result.add("");
            return result;
        }
        if (maxColumns <= 0 || columns(text) <= maxColumns) {
            result.add(text);
            return result;
        }
        StringBuilder current = new StringBuilder();
        float used = 0f;
        for (String token : text.split(" ")) {
            if (token.isEmpty()) {
                continue;
            }
            float width = columns(token);
            if (current.length() > 0 && used + 0.5f + width > maxColumns) {
                result.add(current.toString());
                current.setLength(0);
                used = 0f;
            }
            if (width > maxColumns) { // 单 token 超行宽：字符硬断
                if (current.length() > 0) {
                    result.add(current.toString());
                    current.setLength(0);
                    used = 0f;
                }
                hardSplit(token, maxColumns, result);
                continue;
            }
            if (current.length() > 0) {
                current.append(' ');
                used += 0.5f;
            }
            current.append(token);
            used += width;
        }
        if (current.length() > 0) {
            result.add(current.toString());
        }
        return result;
    }

    private static void hardSplit(String token, int maxColumns, List<String> out) {
        int start = 0;
        while (start < token.length()) {
            int end = start;
            float width = 0f;
            while (end < token.length() && width + charColumns(token.charAt(end)) <= maxColumns) {
                width += charColumns(token.charAt(end));
                end++;
            }
            if (end == start) { // 防零推进（调用方保证 maxColumns ≥ 1）
                end = start + 1;
            }
            out.add(token.substring(start, end));
            start = end;
        }
    }

    /** 列宽估算：全角（含 CJK 与全角标点）= 1 列，半角 = 0.5 列（12px 字体实测口径） */
    public static float columns(String text) {
        float width = 0f;
        for (int i = 0; i < text.length(); i++) {
            width += charColumns(text.charAt(i));
        }
        return width;
    }

    private static float charColumns(char c) {
        return c > 0xFF ? 1f : 0.5f;
    }

    /** 卡高截断（§5.3-4）：超容量行丢弃、末行以 … 示意；capacity ≤ 0 或未超 = 原样返回 */
    public static List<String> clipLines(List<String> lines, int capacity) {
        if (capacity <= 0 || lines.size() <= capacity) {
            return lines;
        }
        List<String> clipped = new ArrayList<String>(lines.subList(0, Math.max(1, capacity - 1)));
        clipped.add("…");
        return clipped;
    }
}
```

- **测试要点**：新建 `render/ui/UnitInfoTextTest`（夹具沿 `UnitDetailDialogTest` 手搓 GameData 先例，羁绊用带 desc 的 6 参构造）——
  - `statLabel`：9 键全断言（HP→生命 … SKILL_POWER→技能强度）；
  - `effectText`：ADD 整数（hp150→生命+150）、PCT（attack20→攻击+20%）、百分比刻度 ADD（lifesteal20→吸血+20%）、浮点（moveSpeed1.0→移速+1）、SHIELD（0.3→护盾30%）；
  - `synergyTierLine`：三档夹具精确串 `2:护甲+20；4:护甲+50·攻击+15%；6:护甲+100·攻击+30%·吸血+20%`；
  - `findSynergy`：RACE 命中 / CLASS 命中 / 风味 key 返回 null；
  - `previewLines`（maxColumns=7）：首行含名与「N费」；属性 3 行；技能名行 + desc 折行（17 字 desc → 3 行且每行 `columns ≤ 7`）；双羁绊块出现、风味种族无块；
  - `detailLines`：首行含「N星（N费）· 累计花费 N」；技能行 `技能 战吼：号令全军，攻击提升`；羁绊行与档位行成对；
  - `wrap`：中英混排、单 token 硬断、maxColumns≤0 原样；`clipLines`：未超原样 / 超容截断末行 `…`。

---

### CP2. SynergyData 增 desc 字段（兼容重载）

- **类型**：修改类（增字段 + 构造重载 + getter）
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/data/SynergyData.java:15-30`（字段与构造段）
- **改动说明**：裁决 2 的代码侧承载。6 参 canonical + 5 参兼容重载（desc=""，沿 Phase 5 CP5 `Unit` 3→4 参先例——§3.2 所列 7 处测试直接构造点零改动）。空 desc 由 `UnitInfoText` 跳过（CP1 防御）。
- **代码**（修改前逐字摘自 SynergyData.java:15-30）：

```java
修改前：
public final class SynergyData {
    private final String id;
    private final String name;
    private final SynergySource source;
    /** 与 units 的 race（source=RACE）或 class（source=CLASS）值精确匹配 */
    private final String key;
    private final List<Threshold> thresholds;

    public SynergyData(String id, String name, SynergySource source, String key,
                       List<Threshold> thresholds) {
        this.id = id;
        this.name = name;
        this.source = source;
        this.key = key;
        this.thresholds = Collections.unmodifiableList(thresholds);
    }
```

```java
修改后：
public final class SynergyData {
    private final String id;
    private final String name;
    /** 一句主题描述（Phase 5.1 裁决 2：synergies.json 必填手写文案；档位数值行由结构化数据生成） */
    private final String desc;
    private final SynergySource source;
    /** 与 units 的 race（source=RACE）或 class（source=CLASS）值精确匹配 */
    private final String key;
    private final List<Threshold> thresholds;

    /** 兼容重载：无 desc（存量测试构造先例）——desc 置空串 */
    public SynergyData(String id, String name, SynergySource source, String key,
                       List<Threshold> thresholds) {
        this(id, name, "", source, key, thresholds);
    }

    public SynergyData(String id, String name, String desc, SynergySource source, String key,
                       List<Threshold> thresholds) {
        this.id = id;
        this.name = name;
        this.desc = desc;
        this.source = source;
        this.key = key;
        this.thresholds = Collections.unmodifiableList(thresholds);
    }
```

（getter 段在 `getName()` 之后插入，修改前逐字摘自 SynergyData.java:33-34）：

```java
修改前：
    public String getId() { return id; }
    public String getName() { return name; }
```

```java
修改后：
    public String getId() { return id; }
    public String getName() { return name; }
    /** 一句主题描述（详情/悬停显示用；兼容重载构造为空串） */
    public String getDesc() { return desc; }
```

- **测试要点**：`data/SynergyDataTest` 增例——6 参构造 desc 透传、`getDesc()` 非空；5 参兼容构造 `getDesc()` 为空串；其余既有断言（档位判定/不可变视图）零回归（验证兼容重载生效）。

---

### CP3. JsonLoader 解析必填 desc（与 skills.json 同口径）

- **类型**：修改方法（parseSynergies）
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/config/JsonLoader.java:261-263`（checkUnknownKeys 与 name）+ :298（构造调用）
- **改动说明**：desc 必填致命（§5.3-9：按 skills.json desc 的 requireString 既有口径类推；结构锁定变更裁决 2 已批）。缺字段抛 `DataValidationException`，报错路径 `synergies.json#<id>/desc`。
- **代码**（修改前逐字摘自 JsonLoader.java:261-264）：

```java
修改前：
            checkUnknownKeys(e, w, "id", "name", "source", "key", "thresholds");

            String name = requireString(e, "name", w);
```

```java
修改后：
            checkUnknownKeys(e, w, "id", "name", "desc", "source", "key", "thresholds");

            String name = requireString(e, "name", w);
            String desc = requireString(e, "desc", w);
```

（构造调用，修改前逐字摘自 JsonLoader.java:298）：

```java
修改前：
            result.put(id, new SynergyData(id, name, source, key, thresholds));
```

```java
修改后：
            result.put(id, new SynergyData(id, name, desc, source, key, thresholds));
```

（同文件夹具适配）`JsonLoaderValidationTest.java:40-44` 的 `VALID_SYNERGY` 两条目补 `"desc": "试作"`（修改前逐字）：

```java
修改前：
    private static final String VALID_SYNERGY =
            "[{ \"id\": \"syn1\", \"name\": \"兽人\", \"source\": \"RACE\", \"key\": \"兽人\","
            + " \"thresholds\": [ { \"count\": 2, \"effects\": [ { \"stat\": \"hp\", \"op\": \"ADD\", \"value\": 10 } ] } ] },"
            + "{ \"id\": \"syn2\", \"name\": \"战士\", \"source\": \"CLASS\", \"key\": \"战士\","
            + " \"thresholds\": [ { \"count\": 2, \"effects\": [ { \"stat\": \"armor\", \"op\": \"ADD\", \"value\": 10 } ] } ] }]";
```

```java
修改后：
    private static final String VALID_SYNERGY =
            "[{ \"id\": \"syn1\", \"name\": \"兽人\", \"desc\": \"试作\", \"source\": \"RACE\", \"key\": \"兽人\","
            + " \"thresholds\": [ { \"count\": 2, \"effects\": [ { \"stat\": \"hp\", \"op\": \"ADD\", \"value\": 10 } ] } ] },"
            + "{ \"id\": \"syn2\", \"name\": \"战士\", \"desc\": \"试作\", \"source\": \"CLASS\", \"key\": \"战士\","
            + " \"thresholds\": [ { \"count\": 2, \"effects\": [ { \"stat\": \"armor\", \"op\": \"ADD\", \"value\": 10 } ] } ] }]";
```

- **测试要点**：`JsonLoaderValidationTest` 增负向例——desc 缺失抛 `DataValidationException` 且报错含 `desc` 路径；既有全部负向/正向用例（依赖 VALID_SYNERGY）回归绿。`JsonLoaderTest` 增正向断言见 CP4。

---

### CP4. synergies.json 六条 desc 手写文案（lore 对齐）

- **类型**：修改资产文件
- **位置**：`assets/data/synergies.json`（全文 6 条；任务描述「12 条」与实测 6 条不符，见 §3.4 #3）
- **改动说明**：裁决 2 手写文案；主题句对齐 `game_lore_design.md:86-91` 种族/职业风味表（评审可对照）。每条一句、不写数值（数值行走档位生成行）。
- **代码**（完整文件）：

```json
[
  { "id": "syn_orc", "name": "兽人", "desc": "焰痕部族的雇佣兵，越战越硬的正面铁壁",
    "source": "RACE", "key": "兽人",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "hp", "op": "ADD", "value": 150 } ] },
      { "count": 4, "effects": [ { "stat": "hp", "op": "ADD", "value": 400 },
                                  { "stat": "attack", "op": "PCT", "value": 20 } ] },
      { "count": 6, "effects": [ { "effect": "SHIELD", "value": 0.3 },
                                  { "stat": "lifesteal", "op": "ADD", "value": 20 } ] }
    ] },
  { "id": "syn_warrior", "name": "战士", "desc": "执旗卫队的老兵，以纪律与护甲结阵而战",
    "source": "CLASS", "key": "战士",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "armor", "op": "ADD", "value": 20 } ] },
      { "count": 4, "effects": [ { "stat": "armor", "op": "ADD", "value": 50 },
                                  { "stat": "attack", "op": "PCT", "value": 15 } ] },
      { "count": 6, "effects": [ { "stat": "armor", "op": "ADD", "value": 100 },
                                  { "stat": "attack", "op": "PCT", "value": 30 },
                                  { "stat": "lifesteal", "op": "ADD", "value": 20 } ] }
    ] },
  { "id": "syn_mage", "name": "法师", "desc": "研究星辉残响的学者，以危险的知识为武器",
    "source": "CLASS", "key": "法师",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "skillPower", "op": "ADD", "value": 15 } ] },
      { "count": 4, "effects": [ { "stat": "skillPower", "op": "ADD", "value": 30 },
                                  { "stat": "energyGainRate", "op": "ADD", "value": 15 } ] },
      { "count": 6, "effects": [ { "stat": "skillPower", "op": "ADD", "value": 50 },
                                  { "stat": "energyGainRate", "op": "ADD", "value": 30 } ] }
    ] },
  { "id": "syn_assassin", "name": "刺客", "desc": "拾星者行会的暗巷传说，一击得手即刻撤离",
    "source": "CLASS", "key": "刺客",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "attack", "op": "PCT", "value": 20 } ] },
      { "count": 4, "effects": [ { "stat": "attack", "op": "PCT", "value": 35 },
                                  { "stat": "moveSpeed", "op": "ADD", "value": 1.0 } ] },
      { "count": 6, "effects": [ { "stat": "attack", "op": "PCT", "value": 50 },
                                  { "stat": "attackSpeed", "op": "PCT", "value": 30 },
                                  { "stat": "moveSpeed", "op": "ADD", "value": 2.0 } ] }
    ] },
  { "id": "syn_beast", "name": "野兽", "desc": "翡翠林地中觉醒的狂血之兽，凭速度与利爪撕开战线",
    "source": "RACE", "key": "野兽",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "attackSpeed", "op": "PCT", "value": 15 } ] },
      { "count": 4, "effects": [ { "stat": "attackSpeed", "op": "PCT", "value": 25 },
                                  { "stat": "moveSpeed", "op": "ADD", "value": 1.0 } ] },
      { "count": 6, "effects": [ { "stat": "attackSpeed", "op": "PCT", "value": 40 },
                                  { "stat": "moveSpeed", "op": "ADD", "value": 2.0 },
                                  { "stat": "attack", "op": "PCT", "value": 15 } ] }
    ] },
  { "id": "syn_ranger", "name": "游侠", "desc": "失去森林的精灵射手，箭术即乡愁",
    "source": "CLASS", "key": "游侠",
    "thresholds": [
      { "count": 2, "effects": [ { "stat": "range", "op": "ADD", "value": 1 } ] },
      { "count": 4, "effects": [ { "stat": "range", "op": "ADD", "value": 2 },
                                  { "stat": "attack", "op": "PCT", "value": 20 } ] },
      { "count": 6, "effects": [ { "stat": "range", "op": "ADD", "value": 2 },
                                  { "stat": "attack", "op": "PCT", "value": 35 },
                                  { "stat": "attackSpeed", "op": "PCT", "value": 15 } ] }
    ] }
]
```

（thresholds/effects 数值与现文件逐字节一致——仅插入 desc 行；格式从现行紧凑单行改为条目展开属排版调整，语义零变化。）

- **测试要点**：`JsonLoaderTest.synergyTiersLoaded`（JsonLoaderTest.java:134-157）增断言——`data.getSynergy("syn_orc").getDesc()` equals CP4 文案、六条 desc 全部非空；`softWarningsOnSeed` 零回归（desc 不触发新告警）。

---

### CP5. HoverStateMachine 悬停驻留状态机（250ms）

- **类型**：新建文件
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/HoverStateMachine.java`
- **改动说明**：裁决 1「悬停 ~250ms 显示」的计时核心（§5.3-2 accumulator 型）。纯逻辑零 Gdx；GL 线程单实例可变（沿 `DragContext` 先例）。状态：ARMING（候选稳定驻留计时，候选变化即重置）→ VISIBLE；任一帧候选为空或 suppressed 立即回隐藏并清零。
- **代码**（完整新建）：

```java
package com.voidvvv.kz_auto_chess_n.render.ui;

/**
 * 悬停显示状态机（Phase 5.1 R1，裁决 A：固定锚悬停卡）：候选源稳定驻留
 * ≥ {@link #DELAY_SECONDS} 才显示。纯逻辑零 Gdx（headless 可测）；
 * GL 线程单实例（沿 BoardInputProcessor.DragContext 可变先例）。
 *
 * <p>状态：ARMING（计时期，候选变化即重置）→ VISIBLE（稳定命中保持显示）；
 * 任一帧候选为空或 suppressed（拖拽/模态/暂停冻结）立即隐藏并清零计时。
 * 抑制条件的施加位置见计划 §5.3-8（查询侧归一，本类只认 candidate 与 suppressed）。
 */
public final class HoverStateMachine {

    /** 悬停驻留阈值（裁决 A：~250ms） */
    public static final float DELAY_SECONDS = 0.25f;

    private int candidate = -1;
    private float elapsed;
    private int visible = -1;

    /**
     * 每帧推进。
     *
     * @param candidate 当前候选源 id（棋盘 unitId / 商店槽位索引；无候选 = -1）
     * @param suppressed 冻结位（BattleScreen frozen：paused || 弹窗模态）
     * @param delta      帧间隔秒（冻结时调用方传 0）
     */
    public void update(int candidate, boolean suppressed, float delta) {
        if (candidate != this.candidate) {
            this.candidate = candidate;
            this.elapsed = 0f;
            this.visible = -1;
        }
        if (candidate < 0 || suppressed) {
            this.elapsed = 0f;
            this.visible = -1;
            return;
        }
        if (this.visible >= 0) {
            return; // 已显示：保持（无需继续累计）
        }
        this.elapsed += delta;
        if (this.elapsed >= DELAY_SECONDS) {
            this.visible = candidate;
        }
    }

    /** 当前可见候选 id（未显示 = -1） */
    public int visibleId() {
        return visible;
    }
}
```

- **测试要点**：新建 `render/ui/HoverStateMachineTest`——
  - 驻留不足不显示（0.1s × 2 帧 → visibleId()==-1）；
  - 累计 ≥ 0.25s 显示（0.13+0.13 → 显示且 == 候选）；
  - 候选变化重置（0.2s 后换候选 → 隐藏、再 0.2s 仍隐藏、补 0.05s 显示新候选）；
  - suppressed 立即隐藏并清零（显示后 suppress 一帧 → -1；解除后需重新计满 0.25s）；
  - candidate=-1 同 suppressed；
  - 显示后持续 update 保持显示（不闪烁）。

---

### CP6. BoardInputProcessor 悬停轨迹与候选查询

- **类型**：修改类（mouseMoved 实现 + 字段 + touchDown 清悬停 + 候选查询方法）
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/input/BoardInputProcessor.java:157-160`（mouseMoved）、:78-94（touchDown）、:52-59（字段段）、类注释 :22-30
- **改动说明**：R1 棋盘域悬停的输入侧。`mouseMoved` 不消费事件（返回 false，后续处理器照常）；`getHoverCandidateUnitId()` 集中施加抑制（§5.3-8）：拖拽中 / modalBlocked / 非 SHOPPING / 名单核验（悬停中棋子被卖出/合并 → -1，防残卡）。按下即清 hoverUnitId（防起手瞬间残卡）。类注释顺带修正 Phase 4 遗留的「模态阻断位……本期常 false」过时句。
- **代码**（字段段，修改前逐字摘自 BoardInputProcessor.java:52-59）：

```java
修改前：
    private final Viewport boardViewport;
    private final CommandManager commandManager;
    private final Supplier<RunContext> context;
    private final BooleanSupplier modalBlocked;
    /** 死区内松手 = 点击棋子的回调（Phase 5：详情面板 / 装备待定态落点；null = 无监听） */
    private final IntConsumer unitClickListener;
    private final Map<Integer, DragContext> drags = new HashMap<Integer, DragContext>();
```

```java
修改后：
    private final Viewport boardViewport;
    private final CommandManager commandManager;
    private final Supplier<RunContext> context;
    private final BooleanSupplier modalBlocked;
    /** 死区内松手 = 点击棋子的回调（Phase 5：详情面板 / 装备待定态落点；null = 无监听） */
    private final IntConsumer unitClickListener;
    /** 悬停中的棋子 id（Phase 5.1 R1：mouseMoved 轨迹维护；-1 = 无；候选抑制见 getHoverCandidateUnitId） */
    private int hoverUnitId = -1;
    private final Map<Integer, DragContext> drags = new HashMap<Integer, DragContext>();
```

（touchDown 尾段，修改前逐字摘自 BoardInputProcessor.java:92-93）：

```java
修改前：
        drags.put(pointer, new DragContext(unit.getId(), placementAt(touch.x, touch.y), touch.x, touch.y));
        return true;
```

```java
修改后：
        drags.put(pointer, new DragContext(unit.getId(), placementAt(touch.x, touch.y), touch.x, touch.y));
        hoverUnitId = -1; // 按下即清悬停（起手瞬间不出残卡；拖拽抑制由候选查询施加）
        return true;
```

（mouseMoved，修改前逐字摘自 BoardInputProcessor.java:157-160）：

```java
修改前：
    @Override
    public boolean mouseMoved(int screenX, int screenY) {
        return false;
    }
```

```java
修改后：
    @Override
    public boolean mouseMoved(int screenX, int screenY) {
        if (modalBlocked.getAsBoolean()) {
            hoverUnitId = -1; // 模态期无悬停（input §3）
            return false;
        }
        RunContext ctx = context.get();
        if (ctx.getRunState().getPhase() != GamePhase.SHOPPING) {
            hoverUnitId = -1; // 悬停仅备战期（R1 抑制条件）
            return false;
        }
        unproject(screenX, screenY);
        Unit unit = unitAt(touch.x, touch.y, ctx);
        hoverUnitId = unit == null ? -1 : unit.getId();
        return false; // 悬停不消费事件（uiStage/后续处理器照常收）
    }

    /** 悬停候选（渲染帧轮询，R1）：拖拽中/模态/非 SHOPPING/名单已无该棋子 → 一律 -1（抑制集中于此） */
    public int getHoverCandidateUnitId() {
        if (isDragging() || modalBlocked.getAsBoolean()) {
            return -1;
        }
        RunContext ctx = context.get();
        if (ctx.getRunState().getPhase() != GamePhase.SHOPPING) {
            return -1;
        }
        Unit unit = ctx.getPlayer().getUnitById(hoverUnitId);
        return unit == null ? -1 : unit.getId();
    }
```

（类注释第三段过时句修正，修改前逐字摘自 BoardInputProcessor.java:28-29）：

```java
修改前：
 * 模态阻断位首行吞事件（本期常 false）。松手才入队（表现层只在合法落点产生命令，
```

```java
修改后：
 * 模态阻断位首行吞事件（BattleScreen 已接 UIDialogManager::isShowing）。松手才入队（表现层只在合法落点产生命令，
```

- **测试要点**：`BoardInputProcessorTest` 增例（IdentityViewport 桩 + shoppingContext 夹具沿用）——
  - `mouseMoved(BENCH0)` → `getHoverCandidateUnitId()==1`；移到空地（如 (400,60) 敌区外）→ -1；
  - 拖拽抑制：touchDown 后 touchDragged 出死区 → 候选 -1；
  - 模态抑制：`modalBlocked=()->true` 构造 → mouseMoved 后候选仍 -1；
  - 非 SHOPPING 抑制：`ctx.getRunState().setPhase(GamePhase.BATTLE)`（RunState.java:63 公有写方法）→ 候选 -1；
  - 名单核验：mouseMoved 后 `player.removeUnit(unit)` → 候选 -1（防残卡）；
  - `mouseMoved` 返回 false（不消费）。

---

### CP7. ShopBar 悬停槽位（enter/exit）+ 按钮与卡面中文化

- **类型**：修改类
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/ShopBar.java:39-67`（字段与构造）、:104-136（ShopCard）
- **改动说明**：R1 商店源（Scene2D enter/exit 维护 hoveredSlot）+ R3 文案中文化与宽度适配（§5.3-7）。`getHoveredSlot()` 原始暴露（-1 = 无），空槽/非 SHOPPING 归一在 HoverPreviewCard（CP8）。enter/exit 在触屏 pointer≥0 也会触发的已知行为记 §8 WARNING-3。
- **代码**（import 段增 `com.badlogic.gdx.scenes.scene2d.InputListener`；字段与构造，修改前逐字摘自 ShopBar.java:39-67）：

```java
修改前：
    private final CommandManager commandManager;
    private final Assets assets;
    private final Supplier<RunContext> context;
    private final boolean[] affordable = new boolean[GameBalance.SHOP_SLOTS];

    public ShopBar(CommandManager commandManager, Assets assets, Supplier<RunContext> context) {
        this.commandManager = commandManager;
        this.assets = assets;
        this.context = context;
        for (int i = 0; i < GameBalance.SHOP_SLOTS; i++) {
            addActor(new ShopCard(i));
        }
        Actor refresh = new ActionButton("REFRESH 2G") {
            @Override
            protected void onClicked() {
                commandManager.addCommand(RefreshShopCommand.INSTANCE);
            }
        };
        refresh.setPosition(BUTTON_X_REFRESH, 14f);
        addActor(refresh);
        Actor exp = new ActionButton("EXP 4G") {
            @Override
            protected void onClicked() {
                commandManager.addCommand(BuyExpCommand.INSTANCE);
            }
        };
        exp.setPosition(BUTTON_X_EXP, 14f);
        addActor(exp);
    }
```

```java
修改后：
    private final CommandManager commandManager;
    private final Assets assets;
    private final Supplier<RunContext> context;
    private final boolean[] affordable = new boolean[GameBalance.SHOP_SLOTS];
    /** 悬停中的商店槽位（Phase 5.1 R1：ShopCard enter/exit 维护；-1 = 无；原始暴露，归一见 HoverPreviewCard） */
    private int hoveredSlot = -1;

    public ShopBar(CommandManager commandManager, Assets assets, Supplier<RunContext> context) {
        this.commandManager = commandManager;
        this.assets = assets;
        this.context = context;
        for (int i = 0; i < GameBalance.SHOP_SLOTS; i++) {
            addActor(new ShopCard(i));
        }
        Actor refresh = new ActionButton("刷新 2金") {
            @Override
            protected void onClicked() {
                commandManager.addCommand(RefreshShopCommand.INSTANCE);
            }
        };
        refresh.setPosition(BUTTON_X_REFRESH, 14f);
        addActor(refresh);
        Actor exp = new ActionButton("经验 4金") {
            @Override
            protected void onClicked() {
                commandManager.addCommand(BuyExpCommand.INSTANCE);
            }
        };
        exp.setPosition(BUTTON_X_EXP, 14f);
        addActor(exp);
    }

    /** 悬停中的商店槽位（R1）；无悬停 = -1（空槽/非 SHOPPING 的归一在 HoverPreviewCard.refresh） */
    public int getHoveredSlot() {
        return hoveredSlot;
    }
```

（ShopCard 构造增悬停监听，修改前逐字摘自 ShopBar.java:107-117）：

```java
修改前：
        ShopCard(int slot) {
            this.slot = slot;
            setSize(CARD_W, CARD_H);
            setPosition(CARD_X0 + slot * (CARD_W + CARD_GAP), 4f);
            addListener(new ClickListener() {
                @Override
                public void clicked(InputEvent event, float x, float y) {
                    commandManager.addCommand(new BuyUnitCommand(slot));
                }
            });
        }
```

```java
修改后：
        ShopCard(int slot) {
            this.slot = slot;
            setSize(CARD_W, CARD_H);
            setPosition(CARD_X0 + slot * (CARD_W + CARD_GAP), 4f);
            addListener(new ClickListener() {
                @Override
                public void clicked(InputEvent event, float x, float y) {
                    commandManager.addCommand(new BuyUnitCommand(slot));
                }
            });
            addListener(new InputListener() { // R1 悬停槽位轨迹（Scene2D enter/exit）
                @Override
                public void enter(InputEvent event, float x, float y, int pointer, Actor fromActor) {
                    hoveredSlot = slot;
                }

                @Override
                public void exit(InputEvent event, float x, float y, int pointer, Actor toActor) {
                    if (hoveredSlot == slot) {
                        hoveredSlot = -1;
                    }
                }
            });
        }
```

（卡面费用中文化，修改前逐字摘自 ShopBar.java:133）：

```java
修改前：
            assets.font().draw(batch, String.valueOf(template.getCost()) + "G", getX() + 44f, getY() + 44f);
```

```java
修改后：
            assets.font().draw(batch, template.getCost() + "金", getX() + 44f, getY() + 44f);
```

（按钮绘字居中适配，修改前逐字摘自 ShopBar.java:161）：

```java
修改前：
            assets.font().draw(batch, text, getX() + 8f, getY() + 22f);
```

```java
修改后：
            assets.font().draw(batch, text, getX() + 16f, getY() + 22f); // 中文化后文案变窄：8 → 16 视觉居中（§5.3-7）
```

- **测试要点**：`ShopBarLogicTest` 既有 `canBuy` 断言零回归（中文化不动逻辑）；enter/exit 轨迹属 Scene2d 事件派发，Actor 构造依赖 Assets（GL）——headless 不测，列入 §7 lwjgl3 手验项 H2。中文字符串无逻辑断言（文案正确性由手验与 CP7 代码评审覆盖）。

---

### CP8. BoardGeometry 悬停锚点常量 + HoverPreviewCard 预览卡组件

- **类型**：修改类（增常量）+ 新建文件
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/board/BoardGeometry.java:51-53`（NOTIFY 与 CELL 之间插入）；新建 `core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/HoverPreviewCard.java`
- **改动说明**：锚点坐标与防遮挡论证见 §5.3-1（评审重点）；卡组件为 uiStage 普通自绘 Actor（非模态瞬态，最上层），行集/截断走 `UnitInfoText`（§6.CP1），计时走 `HoverStateMachine` ×2（§6.CP5，棋盘源优先）。行变更才重建行集（渲染段零稳态分配，沿 TopBar/SynergyPanel 口径）。
- **代码**（BoardGeometry 插入，修改前逐字摘自 BoardGeometry.java:47-53）：

```java
修改前：
    /** ⑨ 事件通知小窗（render §九原值 y=230 与 ③ 底边 240 重叠 10px——差异声明 #4，改 244 起） */
    public static final int NOTIFY_X = 20;
    public static final int NOTIFY_Y = 244;
    public static final int NOTIFY_W = 128;
    public static final int NOTIFY_H = 46;

    public static final int CELL = 32;
```

```java
修改后：
    /** ⑨ 事件通知小窗（render §九原值 y=230 与 ③ 底边 240 重叠 10px——差异声明 #4，改 244 起） */
    public static final int NOTIFY_X = 20;
    public static final int NOTIFY_Y = 244;
    public static final int NOTIFY_W = 128;
    public static final int NOTIFY_H = 46;

    // —— Phase 5.1 R1：悬停预览卡固定锚点（裁决 A；防遮挡论证见计划 §5.3-1） ——

    /** 棋盘域悬停卡：②③（右缘 128）与 ④ 棋盘（左缘 224）之间的左侧空带；底边 240 避开 ⑨（y 244 起）。
     *  瞬态覆盖 ⑥ 开战按钮（x134~198, y88~128）：指针移向按钮即离开棋子命中域 → 卡立即隐藏，无点击阻塞（§5.3-1） */
    public static final int BOARD_HOVER_X = 128;
    public static final int BOARD_HOVER_Y = 48;
    public static final int BOARD_HOVER_W = 94;
    public static final int BOARD_HOVER_H = 192;
    /** 商店卡悬停卡：精确覆盖 ⑤ 羁绊面板区（瞬态覆盖，移开即恢复；⑥ 实际在左侧——差异声明 #2） */
    public static final int SHOP_HOVER_X = 508;
    public static final int SHOP_HOVER_Y = 48;
    public static final int SHOP_HOVER_W = 112;
    public static final int SHOP_HOVER_H = 192;

    public static final int CELL = 32;
```

（HoverPreviewCard 完整新建）：

```java
package com.voidvvv.kz_auto_chess_n.render.ui;

import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.Actor;
import com.voidvvv.kz_auto_chess_n.command.RunContext;
import com.voidvvv.kz_auto_chess_n.data.UnitData;
import com.voidvvv.kz_auto_chess_n.entities.GamePhase;
import com.voidvvv.kz_auto_chess_n.entities.Unit;
import com.voidvvv.kz_auto_chess_n.render.Assets;
import com.voidvvv.kz_auto_chess_n.render.PlaceholderKeys;
import com.voidvvv.kz_auto_chess_n.render.board.BoardGeometry;

import java.util.Collections;
import java.util.List;
import java.util.function.Supplier;

/**
 * 悬停预览卡（Phase 5.1 R1，裁决 A：固定锚、不跟随鼠标、只读精简）。
 * 棋盘域与商店卡各一锚点、各一 {@link HoverStateMachine}（双源互斥——单指针不同时悬停
 * 两处，棋盘源优先）；行集由 {@link UnitInfoText#previewLines} 生成（模板级，不含
 * spend/已穿装备）；超出卡高的行按优先序截断、末行示 …（§5.3-4，完整信息走详情弹窗）。
 * 非模态瞬态：uiStage 最上层普通 Actor，无输入监听、不阻断任何交互。
 */
public final class HoverPreviewCard extends Actor {

    private static final float LINE_HEIGHT = 12f;
    private static final float PADDING_X = 4f;
    private static final float PADDING_TOP = 4f;
    /** 行容量 = floor((H - 顶留 4 - 底 4) / 12)（§5.3-4） */
    private static final int BOARD_LINE_CAPACITY = 15;
    private static final int SHOP_LINE_CAPACITY = 15;
    /** 折行列宽（§5.3-5：(W - 8) / 12 取整） */
    private static final int BOARD_MAX_COLUMNS = 7;
    private static final int SHOP_MAX_COLUMNS = 8;

    private static final Color CARD_TINT = new Color(0.07f, 0.06f, 0.11f, 0.92f);

    private final Assets assets;
    private final Supplier<RunContext> context;
    private final HoverStateMachine boardHover = new HoverStateMachine();
    private final HoverStateMachine shopHover = new HoverStateMachine();
    private List<String> lines = Collections.emptyList();
    private float drawX;
    private float drawY;
    private float drawW;
    private float drawH;

    public HoverPreviewCard(Assets assets, Supplier<RunContext> context) {
        this.assets = assets;
        this.context = context;
    }

    /**
     * 每帧推进（BattleScreen.render 显式调用，delta 显式传入——不依赖 act）。
     *
     * @param boardUnitId 棋盘域悬停候选（§6.CP6 getHoverCandidateUnitId，抑制已在查询侧施加）
     * @param shopSlot    商店悬停槽位（§6.CP7 getHoveredSlot；空槽/非 SHOPPING 在此归一为 -1）
     * @param suppressed  frozen（paused || 弹窗模态）——两源共用抑制位（§5.3-8）
     * @param delta       帧间隔秒（冻结时调用方传 0）
     */
    public void refresh(int boardUnitId, int shopSlot, boolean suppressed, float delta) {
        RunContext ctx = context.get();
        boolean shopping = ctx.getRunState().getPhase() == GamePhase.SHOPPING;
        int board = shopping ? boardUnitId : -1;
        int shop = shopping && shopSlot >= 0 && ctx.getShop().slotAt(shopSlot) != null ? shopSlot : -1;
        boardHover.update(board, suppressed, delta);
        shopHover.update(shop, suppressed, delta);
        recompute(ctx);
    }

    /** 行集与卡位重算（候选可见 id 或模板变化才重建——渲染段零稳态分配） */
    private void recompute(RunContext ctx) {
        UnitData template = resolveTemplate(ctx);
        if (template == null) {
            lines = Collections.emptyList();
            return;
        }
        if (boardHover.visibleId() >= 0) {
            place(BoardGeometry.BOARD_HOVER_X, BoardGeometry.BOARD_HOVER_Y,
                    BoardGeometry.BOARD_HOVER_W, BoardGeometry.BOARD_HOVER_H);
            lines = UnitInfoText.clipLines(
                    UnitInfoText.previewLines(template, ctx.getGameData(), BOARD_MAX_COLUMNS),
                    BOARD_LINE_CAPACITY);
        } else {
            place(BoardGeometry.SHOP_HOVER_X, BoardGeometry.SHOP_HOVER_Y,
                    BoardGeometry.SHOP_HOVER_W, BoardGeometry.SHOP_HOVER_H);
            lines = UnitInfoText.clipLines(
                    UnitInfoText.previewLines(template, ctx.getGameData(), SHOP_MAX_COLUMNS),
                    SHOP_LINE_CAPACITY);
        }
    }

    /** 解析当前应显示的模板（棋盘源优先；名单核验防残卡；空槽归一后商店源必有效） */
    private UnitData resolveTemplate(RunContext ctx) {
        if (boardHover.visibleId() >= 0) {
            Unit unit = ctx.getPlayer().getUnitById(boardHover.visibleId());
            return unit == null ? null : unit.getTemplate();
        }
        if (shopHover.visibleId() >= 0) {
            return ctx.getShop().slotAt(shopHover.visibleId());
        }
        return null;
    }

    private void place(int x, int y, int w, int h) {
        this.drawX = x;
        this.drawY = y;
        this.drawW = w;
        this.drawH = h;
    }

    @Override
    public void draw(Batch batch, float parentAlpha) {
        if (lines.isEmpty()) {
            return;
        }
        Color old = batch.getColor();
        batch.setColor(CARD_TINT.r, CARD_TINT.g, CARD_TINT.b, CARD_TINT.a * parentAlpha);
        batch.draw(assets.region(PlaceholderKeys.PANEL_9SLICE), drawX, drawY, drawW, drawH);
        batch.setColor(old);
        float y = drawY + drawH - PADDING_TOP;
        for (int i = 0; i < lines.size() && y > drawY; i++) {
            assets.font().draw(batch, lines.get(i), drawX + PADDING_X, y);
            y -= LINE_HEIGHT;
        }
    }
}
```

- **测试要点**：`BoardGeometryTest` 增锚点断言——`BOARD_HOVER_X + BOARD_HOVER_W <= BOARD_X`（不压 ④ 棋盘）、`BOARD_HOVER_X >= BENCH_X + BENCH_W`（不压 ②③）、`BOARD_HOVER_Y + BOARD_HOVER_H <= NOTIFY_Y`（不压 ⑨）、`BOARD_HOVER_Y >= BENCH_Y`；`SHOP_HOVER` 与 ⑤ 同位（`SHOP_HOVER_X == SYNERGY_X` 等）、右缘 ≤ VIRTUAL_W。卡本体绘制走 lwjgl3 手验（H1/H2）；行集逻辑已在 CP1 覆盖。

---

### CP9. UnitDetailDialog 信息架构重排（技能/羁绊行 + 中文化）

- **类型**：修改类（全文重排；`UnequipButton` 内部类逻辑不变仅随类保留）
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/UnitDetailDialog.java`（基线 42d9104：背景 380×200、属性行 235/215/198、卸下按钮 x=90 自 y=170 下排、关闭钮 (396,226)）
- **改动说明**：R2 信息架构重排。内容行数增加（标题 1 + 属性 2 + 技能 1 + 羁绊 4 = 8 行）→ 背景自 380×200 增高至 380×252（y 44~296，居中带）；文案行集全部来自 `UnitInfoText.detailLines`（§6.CP1，裁决 1 共用格式化函数）；属性标签中文（§2.1）；装备区下移（x=90 自 y=100 下排 26px 步进——沿用 42d9104 的「卸下按钮自 y 下排」防遮挡方案，与文案区行底 ~152 保留 ≥28px 间距）；关闭钮右上角 (396,272)（顶缘 294 ≤ 背景顶 296）。每帧 refresh 重建行集与卸下按钮（沿 42d9104 幂等重建口径；小量分配沿 SynergyPanel WARNING-5 口径）。
- **代码**（修改前逐字摘自 UnitDetailDialog.java:1-48，import 与类头/字段/构造段）：

```java
修改前：
package com.voidvvv.kz_auto_chess_n.render.ui;

import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.Actor;
import com.badlogic.gdx.scenes.scene2d.Group;
import com.badlogic.gdx.scenes.scene2d.InputEvent;
import com.badlogic.gdx.scenes.scene2d.utils.ClickListener;
import com.voidvvv.kz_auto_chess_n.command.CommandManager;
import com.voidvvv.kz_auto_chess_n.command.RunContext;
import com.voidvvv.kz_auto_chess_n.command.UnequipItemCommand;
import com.voidvvv.kz_auto_chess_n.data.BaseStats;
import com.voidvvv.kz_auto_chess_n.data.UnitData;
import com.voidvvv.kz_auto_chess_n.entities.Equipment;
import com.voidvvv.kz_auto_chess_n.entities.Unit;
import com.voidvvv.kz_auto_chess_n.render.Assets;
import com.voidvvv.kz_auto_chess_n.render.PlaceholderKeys;

import java.util.function.Supplier;

/**
 * 棋子详情弹窗（Phase 4 欠账；dialogStage 弹窗族）。MVP：名/星/spend/模板属性 + 三槽卸下 + 关闭。
 * 每帧 refresh（名单/装备可能经命令变化）；单位已不在名单 → 请求关闭（isExpired）。
 * unitGone 为包级静态纯函数（headless 已测——计划测试要点给出的提取方案）。
 */
public final class UnitDetailDialog extends Group {

    /** 关闭请求（Screen 实现：dialogManager.closeTop） */
    public interface CloseListener {
        void onCloseRequested();
    }

    private final CommandManager commandManager;
    private final Assets assets;
    private final Supplier<RunContext> context;
    private final CloseListener closeListener;
    private int unitId = -1;

    public UnitDetailDialog(CommandManager commandManager, Assets assets,
                            Supplier<RunContext> context, CloseListener closeListener) {
        this.commandManager = commandManager;
        this.assets = assets;
        this.context = context;
        this.closeListener = closeListener;
        Actor close = new CloseButton();
        close.setPosition(396f, 226f);
        addActor(close);
    }
```

```java
修改后：
package com.voidvvv.kz_auto_chess_n.render.ui;

import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.Actor;
import com.badlogic.gdx.scenes.scene2d.Group;
import com.badlogic.gdx.scenes.scene2d.InputEvent;
import com.badlogic.gdx.scenes.scene2d.utils.ClickListener;
import com.voidvvv.kz_auto_chess_n.command.CommandManager;
import com.voidvvv.kz_auto_chess_n.command.RunContext;
import com.voidvvv.kz_auto_chess_n.command.UnequipItemCommand;
import com.voidvvv.kz_auto_chess_n.entities.Equipment;
import com.voidvvv.kz_auto_chess_n.entities.Unit;
import com.voidvvv.kz_auto_chess_n.render.Assets;
import com.voidvvv.kz_auto_chess_n.render.PlaceholderKeys;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Supplier;

/**
 * 棋子详情弹窗（Phase 5.1 R2 信息架构重排，基线 42d9104 布局修复版）：
 * 标题（名/星/费/累计花费）→ 属性两行 → 技能（名：desc）→ 两条羁绊（desc + 档位行，R2 混合口径）
 * → 装备区（三槽卸下按钮 x=90 自 y=100 下排 26px 步进）→ 关闭（右上角）。
 * 文案行集由 {@link UnitInfoText#detailLines} 生成（每帧 refresh 重建——幂等轻量重建，
 * 小量分配沿 SynergyPanel 口径）；单位已不在名单 → 请求关闭（isExpired）。
 */
public final class UnitDetailDialog extends Group {

    /** 弹窗几何（42d9104 的 380×200 增高至 252：内容行数 3 → 8） */
    private static final float BG_X = 70f;
    private static final float BG_Y = 44f;
    private static final float BG_W = 380f;
    private static final float BG_H = 252f;
    private static final float TEXT_X = 90f;
    private static final float TITLE_Y = 282f;
    private static final float LINE_HEIGHT = 16f;
    /** 装备区（42d9104 防遮挡方案的延续：卸下按钮自 y 下排，与文案区行底留 ≥28px） */
    private static final float EQUIP_X = 90f;
    private static final float EQUIP_Y0 = 100f;
    private static final float EQUIP_STEP = 26f;

    /** 关闭请求（Screen 实现：dialogManager.closeTop） */
    public interface CloseListener {
        void onCloseRequested();
    }

    private final CommandManager commandManager;
    private final Assets assets;
    private final Supplier<RunContext> context;
    private final CloseListener closeListener;
    private int unitId = -1;
    private List<String> lines = new ArrayList<String>();

    public UnitDetailDialog(CommandManager commandManager, Assets assets,
                            Supplier<RunContext> context, CloseListener closeListener) {
        this.commandManager = commandManager;
        this.assets = assets;
        this.context = context;
        this.closeListener = closeListener;
        Actor close = new CloseButton();
        close.setPosition(396f, 272f); // 右上角（顶缘 294 ≤ 背景顶 296）
        addActor(close);
    }
```

（refresh，修改前逐字摘自 UnitDetailDialog.java:65-83）：

```java
修改前：
    /** 每帧刷新：三槽卸下按钮重建（装备集变化幂等——按钮为轻量 Actor） */
    public void refresh() {
        for (int i = getChildren().size - 1; i >= 0; i--) {
            if (getChildren().get(i) instanceof UnequipButton) {
                getChildren().get(i).remove();
            }
        }
        Unit unit = context.get().getPlayer().getUnitById(unitId);
        if (unit == null) {
            return;
        }
        float y = 170f;
        for (Equipment item : unit.getEquipped()) {
            UnequipButton button = new UnequipButton(item);
            button.setPosition(90f, y);
            addActor(button);
            y -= 30f;
        }
    }
```

```java
修改后：
    /** 每帧刷新：文案行重建 + 三槽卸下按钮重建（名单/装备可能经命令变化——幂等轻量重建） */
    public void refresh() {
        for (int i = getChildren().size - 1; i >= 0; i--) {
            if (getChildren().get(i) instanceof UnequipButton) {
                getChildren().get(i).remove();
            }
        }
        Unit unit = context.get().getPlayer().getUnitById(unitId);
        if (unit == null) {
            lines = new ArrayList<String>();
            return;
        }
        lines = UnitInfoText.detailLines(unit, context.get().getGameData());
        float y = EQUIP_Y0;
        for (Equipment item : unit.getEquipped()) {
            UnequipButton button = new UnequipButton(item);
            button.setPosition(EQUIP_X, y);
            addActor(button);
            y -= EQUIP_STEP;
        }
    }
```

（draw，修改前逐字摘自 UnitDetailDialog.java:85-104）：

```java
修改前：
    @Override
    public void draw(Batch batch, float parentAlpha) {
        Unit unit = context.get().getPlayer().getUnitById(unitId);
        if (unit == null) {
            return;
        }
        Color old = batch.getColor();
        batch.setColor(0f, 0f, 0f, 0.75f * parentAlpha);
        batch.draw(assets.region(PlaceholderKeys.WHITE), 70f, 50f, 380f, 200f);
        batch.setColor(old);
        UnitData template = unit.getTemplate();
        BaseStats stats = template.getBaseStats();
        assets.font().draw(batch, template.getName() + "  " + unit.getStar() + "星"
                + "  spend " + unit.getSpend(), 90f, 235f);
        assets.font().draw(batch, "HP " + stats.getHp() + "  ATK " + stats.getAttack()
                + "  ARMOR " + stats.getArmor(), 90f, 215f);
        assets.font().draw(batch, "ASPD " + stats.getAttackSpeed() + "  RANGE " + stats.getRange()
                + "  MSPD " + stats.getMoveSpeed(), 90f, 198f);
        super.draw(batch, parentAlpha); // 卸下/关闭按钮
    }
```

```java
修改后：
    @Override
    public void draw(Batch batch, float parentAlpha) {
        Unit unit = context.get().getPlayer().getUnitById(unitId);
        if (unit == null) {
            return;
        }
        Color old = batch.getColor();
        batch.setColor(0f, 0f, 0f, 0.75f * parentAlpha);
        batch.draw(assets.region(PlaceholderKeys.WHITE), BG_X, BG_Y, BG_W, BG_H);
        batch.setColor(old);
        float y = TITLE_Y;
        for (int i = 0; i < lines.size(); i++) { // 行集：UnitInfoText.detailLines（中文化+技能/羁绊）
            assets.font().draw(batch, lines.get(i), TEXT_X, y);
            y -= LINE_HEIGHT;
        }
        super.draw(batch, parentAlpha); // 卸下/关闭按钮
    }
```

（CloseButton 绘字，修改前逐字摘自 UnitDetailDialog.java:123）：

```java
修改前：
            assets.font().draw(batch, "X", getX() + 20f, getY() + 15f);
```

```java
修改后：
            assets.font().draw(batch, "关闭", getX() + 12f, getY() + 15f);
```

（`showUnit`/`isExpired`/`unitGone`/`CloseButton` 结构/`UnequipButton` 全类保持不变——`UnequipButton` 绘字 `item… + "  [卸下]"` 已中文。）

- **测试要点**：`UnitDetailDialogTest` 既有 `unitGone` 四例零回归（纯函数未动）；文案内容断言在 `UnitInfoTextTest.detailLines`（CP1）覆盖；布局/遮挡走 lwjgl3 手验项 H3。

---

### CP10. TopBar statusText 中文化 + TopBarTextTest 改写

- **类型**：修改方法 + 改写测试
- **位置**：`core/src/main/java/com/voidvvv/kz_auto_chess_n/render/ui/TopBar.java:53-60`；`core/src/test/java/com/voidvvv/kz_auto_chess_n/render/ui/TopBarTextTest.java:14-35`
- **改动说明**：R3 顶栏中文化（§2.1 术语：轮次/金币/等级/满级）。`statusText` 仍为纯函数（Label setText 判定基准不变）；中文文案约 180px 宽，与右侧暂停钮（x=584）无碰撞（§5.3-7）。
- **代码**（修改前逐字摘自 TopBar.java:53-60）：

```java
修改前：
    /** ① 区文案数据源（纯函数；expToNextLevel(Lv.7)=0 → MAX） */
    static String statusText(int round, int gold, int level, int currentExp) {
        int need = GameBalance.expToNextLevel(level);
        String exp = need == 0 ? "MAX" : currentExp + "/" + need;
        return "ROUND " + round + "/" + GameBalance.TOTAL_ROUNDS
                + "  GOLD " + gold
                + "  LV " + level + " (" + exp + ")";
    }
```

```java
修改后：
    /** ① 区文案数据源（纯函数；expToNextLevel(Lv.7)=0 → 满级；术语见计划 §2.1） */
    static String statusText(int round, int gold, int level, int currentExp) {
        int need = GameBalance.expToNextLevel(level);
        String exp = need == 0 ? "满级" : currentExp + "/" + need;
        return "轮次 " + round + "/" + GameBalance.TOTAL_ROUNDS
                + "  金币 " + gold
                + "  等级 " + level + "（" + exp + "）";
    }
```

（TopBarTextTest 断言改写，修改前逐字摘自 TopBarTextTest.java:14-35）：

```java
修改前：
    @Test
    @DisplayName("开局文案：ROUND 1/25 + GOLD 10 + LV 1 (0/4)")
    void openingText() {
        assertThat(TopBar.statusText(1, 10, 1, 0))
                .isEqualTo("ROUND 1/25  GOLD 10  LV 1 (0/4)");
    }

    @Test
    @DisplayName("Lv.7 封顶显示 MAX（expToNextLevel(7)=0）")
    void maxLevelShowsMax() {
        assertThat(TopBar.statusText(7, 33, 7, 0))
                .isEqualTo("ROUND 7/25  GOLD 33  LV 7 (MAX)");
    }

    @Test
    @DisplayName("中期数值随动：round/金币/经验进度逐字段核对")
    void midRunFields() {
        assertThat(TopBar.statusText(25, 0, 3, 5))
                .isEqualTo("ROUND 25/25  GOLD 0  LV 3 (5/16)");
        assertThat(TopBar.statusText(12, 47, 6, 55))
                .isEqualTo("ROUND 12/25  GOLD 47  LV 6 (55/56)");
    }
```

```java
修改后：
    @Test
    @DisplayName("开局文案：轮次 1/25 + 金币 10 + 等级 1（0/4）")
    void openingText() {
        assertThat(TopBar.statusText(1, 10, 1, 0))
                .isEqualTo("轮次 1/25  金币 10  等级 1（0/4）");
    }

    @Test
    @DisplayName("Lv.7 封顶显示 满级（expToNextLevel(7)=0）")
    void maxLevelShowsMax() {
        assertThat(TopBar.statusText(7, 33, 7, 0))
                .isEqualTo("轮次 7/25  金币 33  等级 7（满级）");
    }

    @Test
    @DisplayName("中期数值随动：round/金币/经验进度逐字段核对")
    void midRunFields() {
        assertThat(TopBar.statusText(25, 0, 3, 5))
                .isEqualTo("轮次 25/25  金币 0  等级 3（5/16）");
        assertThat(TopBar.statusText(12, 47, 6, 55))
                .isEqualTo("轮次 12/25  金币 47  等级 6（55/56）");
    }
```

（`deterministicForSameInputs` 用例不变——同值相等与文案无关。）

- **测试要点**：即上述改写（TDD：先改断言跑红，再改 statusText 跑绿）；`GameBalanceTest` 零关联。

---

### CP11. BattleHud / ShoppingHud 中文化（速度 / 投降 / 开战）

- **类型**：修改方法 ×3
- **位置**：`render/ui/BattleHud.java:83`（SpeedButton.draw 绘字）、`render/ui/BattleHud.java:122`（GiveUpButton.draw 绘字）、`render/ui/ShoppingHud.java:49`（FightButton.draw 绘字）
- **改动说明**：R3 战斗 HUD 与开战按钮中文化（§2.1：速度 x1/x2、投降、开战）。按钮尺寸不变（76/96/64 宽），绘字偏移按中文宽度居中微调（§5.3-7）。
- **代码**（BattleHud 速度按钮，修改前逐字摘自 BattleHud.java:83-84）：

```java
修改前：
            assets.font().draw(batch, speedFactor == 1f ? "SPD x1" : "SPD x2",
                    getX() + 6f, getY() + 24f);
```

```java
修改后：
            assets.font().draw(batch, speedFactor == 1f ? "速度 x1" : "速度 x2",
                    getX() + 16f, getY() + 24f);
```

（BattleHud 投降按钮，修改前逐字摘自 BattleHud.java:122）：

```java
修改前：
            assets.font().draw(batch, "GIVE UP", getX() + 18f, getY() + 24f);
```

```java
修改后：
            assets.font().draw(batch, "投降", getX() + 36f, getY() + 24f);
```

（ShoppingHud 开战按钮，修改前逐字摘自 ShoppingHud.java:49）：

```java
修改前：
            assets.font().draw(batch, "FIGHT", getX() + 10f, getY() + 24f);
```

```java
修改后：
            assets.font().draw(batch, "开战", getX() + 20f, getY() + 24f);
```

- **测试要点**：无 headless 断言（纯绘字）；走 lwjgl3 手验项 H4（三按钮文案与居中、点击行为不变——投降命令路径与变速回调已有既有行为，零逻辑改动）。

---

### CP12. BattleRenderer 中文化（出售区 / 备战引导）与过时注释修正

- **类型**：修改字段 + 修改方法 ×2 + 注释修正
- **位置**：`render/board/BattleRenderer.java:38`（SHOPPING_HINT）、:207（SELL 绘字）、:210（drawShoppingHint 注释）
- **改动说明**：R3 出售区与引导文案中文化。简报已指明「原『默认字体无 CJK 字模』注释已过时，Fusion Pixel 已入库」——注释一并修正（引导文案改中文后该注释必须更新，否则误导）。中文引导约 230px，居中绘制逻辑（GlyphLayout 缓存）不变。
- **代码**（常量，修改前逐字摘自 BattleRenderer.java:37-38）：

```java
修改前：
    /** 备战期引导文案（仅 SHOPPING 期可见，drawShopping 尾部） */
    private static final String SHOPPING_HINT = "DRAG UNITS TO BOARD · DRAG TO SELL · THEN FIGHT";
```

```java
修改后：
    /** 备战期引导文案（仅 SHOPPING 期可见，drawShopping 尾部；中文化——Fusion Pixel 已入库，降级口径见计划 §5.3-6） */
    private static final String SHOPPING_HINT = "拖拽棋子布阵 · 拖至右下出售区卖出 · 点开战";
```

（出售区绘字，修改前逐字摘自 BattleRenderer.java:207）：

```java
修改前：
        assets.font().draw(batch, "SELL", BoardGeometry.SELL_ZONE_X + 10f, BoardGeometry.SELL_ZONE_Y + 28f);
```

```java
修改后：
        assets.font().draw(batch, "出售", BoardGeometry.SELL_ZONE_X + 16f, BoardGeometry.SELL_ZONE_Y + 28f);
```

（过时注释修正，修改前逐字摘自 BattleRenderer.java:210）：

```java
修改前：
    /** 备战期引导文案（非 CJK 字符——默认字体无 CJK 字模；棋盘上方居中，不遮挡棋盘/备战席/⑧ 商店栏，feedback01 修正） */
```

```java
修改后：
    /** 备战期引导文案（棋盘上方居中，不遮挡棋盘/备战席/⑧ 商店栏，feedback01 修正；字体缺文件回退时中文不渲染但不炸——§5.3-6） */
```

- **测试要点**：无 headless 断言（纯绘字与常量）；lwjgl3 手验项 H5（引导文案居中完整、出售区两字居中、GlyphLayout 缓存首帧构建正常）。

---

### CP13. ResultBanner / RunEndPanel 中文化（胜负 / 终局）

- **类型**：修改方法 ×2 + 绘字定位调整
- **位置**：`render/ui/ResultBanner.java:50-63`（refresh 文案）、:75/:78（draw 绘字 x）；`render/ui/RunEndPanel.java:50-55`（draw 文案与定位）、:75（RESTART）
- **改动说明**：R3 胜负横幅与终局面板中文化（§2.1：胜利/战败/超时/选择一个宝箱/点击任意处重试/远征通关/远征已放弃/重新开始）。中文宽度与英文不同 → 绘字 x 按 400px 横幅/460px 面板居中重算（估算值，手验微调；scale2 标题 4 字 ×24 ≈ 96px）。
- **代码**（ResultBanner refresh，修改前逐字摘自 ResultBanner.java:50-63）：

```java
修改前：
    /** 每帧刷新文案（RESULT 期由 Screen 调用；mercyLine 可 null——败局怜悯提示） */
    public void refresh(BattleOutcome outcome, String mercyLine) {
        if (outcome == BattleOutcome.PLAYER_WIN) {
            text = "VICTORY";
            tint = Color.GREEN;
            hint = "pick a chest"; // 胜局唯一出口 = PickChest（口径 #9，无自动推进）
        } else if (outcome == BattleOutcome.ENEMY_WIN) {
            text = "DEFEAT";
            tint = Color.RED;
            hint = mercyLine != null ? "click to retry · " + mercyLine : "click to retry";
        } else {
            text = "TIMEOUT";
            tint = Color.YELLOW;
            hint = mercyLine != null ? "click to retry · " + mercyLine : "click to retry";
        }
    }
```

```java
修改后：
    /** 每帧刷新文案（RESULT 期由 Screen 调用；mercyLine 可 null——败局怜悯提示；术语见计划 §2.1） */
    public void refresh(BattleOutcome outcome, String mercyLine) {
        if (outcome == BattleOutcome.PLAYER_WIN) {
            text = "胜利";
            tint = Color.GREEN;
            hint = "选择一个宝箱"; // 胜局唯一出口 = PickChest（口径 #9，无自动推进）
        } else if (outcome == BattleOutcome.ENEMY_WIN) {
            text = "战败";
            tint = Color.RED;
            hint = mercyLine != null ? "点击任意处重试 · " + mercyLine : "点击任意处重试";
        } else {
            text = "超时";
            tint = Color.YELLOW;
            hint = mercyLine != null ? "点击任意处重试 · " + mercyLine : "点击任意处重试";
        }
    }
```

（ResultBanner draw 绘字定位，修改前逐字摘自 ResultBanner.java:75-78）：

```java
修改前：
        assets.font().draw(batch, text, 270f, 185f);
        assets.font().getData().setScale(1f);
        assets.font().setColor(Color.WHITE);
        assets.font().draw(batch, hint, 258f, 150f);
```

```java
修改后：
        assets.font().draw(batch, text, 296f, 185f); // 2 字 ×24px 居中（原 7 字 ×~24px 的 270）
        assets.font().getData().setScale(1f);
        assets.font().setColor(Color.WHITE);
        assets.font().draw(batch, hint, 272f, 150f); // 提示行居中基准（怜悯长行手验微调）
```

（RunEndPanel draw，修改前逐字摘自 RunEndPanel.java:50-55）：

```java
修改前：
        assets.font().draw(batch, abandoned ? "RUN ABANDONED" : "RUN COMPLETE", abandoned ? 215f : 225f, 230f);
        assets.font().getData().setScale(1f);
        int round = ctx.getRunState().getRound();
        assets.font().draw(batch, "survived to round " + round + "/" + GameBalance.TOTAL_ROUNDS, 240f, 200f);
        assets.font().draw(batch, "mastery +" + ctx.getRunState().getMasteryAwarded() + " (stub, Phase 6)", 235f, 180f);
        assets.font().draw(batch, "seed " + ctx.getRunState().getSeed(), 262f, 160f);
```

```java
修改后：
        assets.font().draw(batch, abandoned ? "远征已放弃" : "远征通关", 272f, 230f); // 4 字 ×24px 居中
        assets.font().getData().setScale(1f);
        int round = ctx.getRunState().getRound();
        assets.font().draw(batch, "抵达第 " + round + "/" + GameBalance.TOTAL_ROUNDS + " 轮", 280f, 200f);
        assets.font().draw(batch, "熟练度 +" + ctx.getRunState().getMasteryAwarded() + "（Phase 6 接档案）", 262f, 180f);
        assets.font().draw(batch, "种子 " + ctx.getRunState().getSeed(), 285f, 160f);
```

（RESTART 按钮，修改前逐字摘自 RunEndPanel.java:75）：

```java
修改前：
            assets.font().draw(batch, "RESTART", getX() + 34f, getY() + 23f);
```

```java
修改后：
            assets.font().draw(batch, "重新开始", getX() + 46f, getY() + 23f);
```

- **测试要点**：无 headless 断言（纯文案/绘字）；lwjgl3 手验项 H6（三态横幅、怜悯长行不越 400px 横幅底板、终局四行居中、重开按钮行为不变——`restartRun` 既有链路零改动）。

---

### CP14. MainMenuScreen / LoadingScreen 中文化（开始远征 / 余烬军团）

- **类型**：修改方法 ×2 + 注释修正
- **位置**：`screens/MainMenuScreen.java:93`（START 绘字）；`screens/LoadingScreen.java:46-47`（标题/装载）+ :15-16（注释）
- **改动说明**：R3 全 UI 统一（裁决 3）覆盖主菜单与装载屏。装载屏仅 2 帧，CJK 降级为瞬时空白（§5.3-6，不炸）。过时注释「本期屏显文案用 ASCII（Q4：无 CJK 字模）」一并修正（差异声明 #6）。
- **代码**（MainMenuScreen，修改前逐字摘自 MainMenuScreen.java:93）：

```java
修改前：
            assets.font().draw(batch, "START", getX() + 58f, getY() + 23f);
```

```java
修改后：
            assets.font().draw(batch, "开始远征", getX() + 56f, getY() + 23f); // 4 字 ×12px 居中于 160 宽钮
```

（LoadingScreen 绘字，修改前逐字摘自 LoadingScreen.java:46-47）：

```java
修改前：
        assets.font().draw(batch, "EMBER LEGION", 40f, BoardGeometry.VIRTUAL_H / 2f + 20f);
        assets.font().draw(batch, "loading...", 60f, BoardGeometry.VIRTUAL_H / 2f - 10f);
```

```java
修改后：
        assets.font().draw(batch, "余烬军团", 296f, BoardGeometry.VIRTUAL_H / 2f + 20f); // 4 字居中（lore 暂定名）
        assets.font().draw(batch, "装载中……", 293f, BoardGeometry.VIRTUAL_H / 2f - 10f);
```

（LoadingScreen 注释，修改前逐字摘自 LoadingScreen.java:15-16）：

```java
修改前：
 * 此处首帧绘出提示后切主菜单（"首帧完成占位生成后切 MainMenu"）。文字用内置默认字体
 * （Q4：无 CJK 字模，本期屏显文案用 ASCII，观感降级已知）。
```

```java
修改后：
 * 此处首帧绘出提示后切主菜单（"首帧完成占位生成后切 MainMenu"）。字体经 Assets.font()
 * 加载 Fusion Pixel（已入库）；缺文件回退内置默认时中文不渲染但不炸（计划 §5.3-6）。
```

- **测试要点**：无 headless 断言（纯绘字）；lwjgl3 手验项 H7（主菜单按钮文案/点击进战斗屏不变；装载屏两帧中文可见——顺带验证字体加载）。

---

### CP15. NotificationPanel CAST 行技能名中文化

- **类型**：修改方法（formatEvent 签名扩展）+ 调用点 + 测试改写
- **位置**：`render/ui/NotificationPanel.java:67-75`（refresh 调用点）、:91-100（formatEvent）；`render/ui/NotificationFormatTest.java:54-59`
- **改动说明**：R3 收尾——CAST 行现显示英文数据 id（`技能施放: skill_fireball`），改为经 `GameData` 查表显中文技能名（查表失败回退 id，防御）。`formatEvent` 为包级静态纯函数（NotificationFormatTest.java:50-66 直测先例），签名加 `GameData` 参数。
- **代码**（调用点，修改前逐字摘自 NotificationPanel.java:67-75）：

```java
修改前：
        inbox.forEachNew(new Consumer<CombatEvent>() {
            @Override
            public void accept(CombatEvent event) {
                String line = formatEvent(event);
                if (line != null && log.appendCapped(line, appended[0])) {
                    appended[0]++; // 超限丢弃（§5.5 防刷屏，WARNING-6）
                }
            }
        });
```

```java
修改后：
        inbox.forEachNew(new Consumer<CombatEvent>() {
            @Override
            public void accept(CombatEvent event) {
                String line = formatEvent(event, ctx.getGameData());
                if (line != null && log.appendCapped(line, appended[0])) {
                    appended[0]++; // 超限丢弃（§5.5 防刷屏，WARNING-6）
                }
            }
        });
```

（formatEvent，修改前逐字摘自 NotificationPanel.java:90-100）：

```java
修改前：
    /** 战斗事件行（仅 UNIT_DIED/CAST——HIT/HEALED 过噪跳过，口径 #13） */
    static String formatEvent(CombatEvent event) {
        switch (event.getType()) {
            case UNIT_DIED:
                return "单位倒下";
            case CAST:
                return "技能施放: " + event.getSkillId();
            default:
                return null;
        }
    }
```

```java
修改后：
    /** 战斗事件行（仅 UNIT_DIED/CAST——HIT/HEALED 过噪跳过，口径 #13；技能行显中文名，查表失败回退 id） */
    static String formatEvent(CombatEvent event, GameData data) {
        switch (event.getType()) {
            case UNIT_DIED:
                return "单位倒下";
            case CAST:
                return "技能施放：" + skillName(data, event.getSkillId());
            default:
                return null;
        }
    }

    /** 技能中文名（GameData 查表；未登记 id 回退原值——防御，正常路径加载期已校验存在） */
    private static String skillName(GameData data, String skillId) {
        SkillData skill = data.getSkill(skillId);
        return skill != null ? skill.getName() : skillId;
    }
```

（import 段增 `com.voidvvv.kz_auto_chess_n.data.GameData` 与 `com.voidvvv.kz_auto_chess_n.data.SkillData`。）

（NotificationFormatTest CAST 用例改写 + 增回退用例，修改前逐字摘自 NotificationFormatTest.java:54-59）：

```java
修改前：
    @Test
    @DisplayName("战斗事件行：CAST 带技能 id")
    void castLineWithSkillId() {
        assertThat(NotificationPanel.formatEvent(CombatEvent.cast(3, 1, 2, "skill_fireball")))
                .isEqualTo("技能施放: skill_fireball");
    }
```

```java
修改后：
    @Test
    @DisplayName("战斗事件行：CAST 显中文技能名（GameData 查表）")
    void castLineWithSkillName() {
        GameData data = new GameData(new java.util.LinkedHashMap<String, UnitData>(),
                skillsOf("skill_fireball", "火球术"),
                new java.util.LinkedHashMap<String, SynergyData>(),
                new java.util.LinkedHashMap<String, SceneData>(),
                new java.util.LinkedHashMap<String, EquipmentData>(),
                new java.util.ArrayList<String>());
        assertThat(NotificationPanel.formatEvent(
                CombatEvent.cast(3, 1, 2, "skill_fireball"), data))
                .isEqualTo("技能施放：火球术");
    }

    @Test
    @DisplayName("战斗事件行：CAST 查表失败回退原始 id（防御路径）")
    void castLineFallsBackToId() {
        assertThat(NotificationPanel.formatEvent(
                CombatEvent.cast(3, 1, 2, "skill_ghost"), emptyData()))
                .isEqualTo("技能施放：skill_ghost");
    }
```

（测试内新增 `skillsOf`/`emptyData` 私有夹具方法，沿 `UnitDetailDialogTest.context` 的 GameData 六参构造先例；import 增 GameData/SkillData/UnitData/SynergyData/SceneData/EquipmentData。）

- **测试要点**：即上述改写（TDD 先行）；`UNIT_DIED`/`HIT` 用例改传 `emptyData()` 后断言不变。

---

### CP16. BattleScreen 装配悬停预览卡

- **类型**：修改类（字段 + 构造 + render 挂接）
- **位置**：`screens/BattleScreen.java:92-94`（字段段）、:143（构造）、:178（uiStage.addActor 序）、:261-262（render）
- **改动说明**：悬停链路的装配收口（screens 是唯一装配点）。`hoverPreview` 为 uiStage 最上层（addActor 序最后 = 绘制最上、且无输入监听不阻断交互）；每帧显式 `refresh(棋盘候选, 商店槽位, frozen, frozen ? 0 : delta)`——`frozen` 即 render 既有局部变量（BattleScreen.java:240，paused || 弹窗模态），抑制位与模拟冻结同源（§5.3-8）。`boardProcessor` 在 `show()` 才创建（构造期 null → 传 -1，首帧无卡）。
- **代码**（字段段，修改前逐字摘自 BattleScreen.java:92-94）：

```java
修改前：
    private final SynergyPanel synergyPanel;
    private final NotificationPanel notificationPanel;
    private final ChestDialog chestDialog;
```

```java
修改后：
    private final SynergyPanel synergyPanel;
    private final NotificationPanel notificationPanel;
    private final HoverPreviewCard hoverPreview; // Phase 5.1 R1：悬停预览卡（uiStage 最上层瞬态）
    private final ChestDialog chestDialog;
```

（import 段增 `com.voidvvv.kz_auto_chess_n.render.ui.HoverPreviewCard`。）

（构造，修改前逐字摘自 BattleScreen.java:143）：

```java
修改前：
        this.notificationPanel = new NotificationPanel(assets, contextSupplier(), commandManager);
```

```java
修改后：
        this.notificationPanel = new NotificationPanel(assets, contextSupplier(), commandManager);
        this.hoverPreview = new HoverPreviewCard(assets, contextSupplier());
```

（uiStage 装配序，修改前逐字摘自 BattleScreen.java:178）：

```java
修改前：
        uiStage.addActor(notificationPanel);
```

```java
修改后：
        uiStage.addActor(notificationPanel);
        uiStage.addActor(hoverPreview); // 最上层：瞬态悬停卡（无输入监听，不阻断任何交互）
```

（render 挂接，修改前逐字摘自 BattleScreen.java:261-262）：

```java
修改前：
        notificationPanel.refresh(runContext);
        notificationPanel.syncBattle(runContext.getBattleState());
```

```java
修改后：
        notificationPanel.refresh(runContext);
        notificationPanel.syncBattle(runContext.getBattleState());
        hoverPreview.refresh(boardProcessor == null ? -1 : boardProcessor.getHoverCandidateUnitId(),
                shopBar.getHoveredSlot(), frozen, frozen ? 0f : delta); // R1：候选/槽位/冻结位（§5.3-8）
```

- **测试要点**：装配属 GL 装配点（headless 不测 Screen）；抑制矩阵与计时已在 CP5/CP6 覆盖；lwjgl3 手验项 H1/H2 全链验证。

---

## 7. 分阶段任务拆解（executor 按序执行）

| 任务 | 所含 CP | 前置 | 验收标准 |
|------|---------|------|----------|
| T1 数据层 desc | CP2 → CP3 → CP4 | — | `JsonLoaderTest`（含新 desc 断言）与 `JsonLoaderValidationTest`（含缺 desc 负向例）全绿；真种子加载零新增告警；`SynergyDataTest` 兼容重载用例绿 |
| T2 文案格式化核心 | CP1 | T1（消费 desc） | `UnitInfoTextTest` 全绿（词表/档位行/查羁绊/行集/折行/截断六组）；纯函数零 Gdx 依赖（import 无 com.badlogic.gdx） |
| T3 悬停输入 | CP5、CP6 | — | `HoverStateMachineTest` 六态全绿；`BoardInputProcessorTest` 增例全绿（含四类抑制与 mouseMoved 不消费） |
| T4 悬停卡与装配 | CP7、CP8、CP16 | T2、T3 | `BoardGeometryTest` 锚点断言绿；编译通过（BattleScreen 装配）；lwjgl3 手验 H1/H2 通过 |
| T5 详情弹窗重排 | CP9 | T2 | `UnitDetailDialogTest` 零回归；`UnitInfoTextTest.detailLines` 断言绿；lwjgl3 手验 H3 通过 |
| T6 全 UI 中文化 | CP10、CP11、CP12、CP13、CP14、CP15（CP7 按钮部分已随 T4） | — | `TopBarTextTest`/`NotificationFormatTest` 改写后全绿；`grep` 复核 §5.4 清单 1~17 项英文清零（保留项：`II` 符号、键名/数字）；lwjgl3 手验 H4~H7 通过 |
| T7 全量回归与手验 | — | T1~T6 | gradle 全量测试 XML 核对（564+，零失败）；§7 手验清单 H1~H7 全过；无 RNG 消耗点变化（`getConsumedCount()` 相关既有断言零改动即证） |

**lwjgl3 手验清单**：

- H1 棋盘悬停：备战期鼠标悬停棋盘/备战席棋子 ~0.25s 出卡（左侧空带锚点），内容含名/费/属性/技能/羁绊；移开即隐；同格微移不闪烁；拖拽中不出卡；开详情弹窗（模态）后不出卡；开战（BATTLE）后不出卡。
- H2 商店悬停：悬停商店卡 ~0.25s 出卡（右侧 ⑤ 位锚点）；已购空槽不出卡；移到刷新/经验按钮不出卡。
- H3 详情弹窗：标题（名/星/费/累计花费）+ 中文属性两行 + 技能行 + 两条羁绊 desc/档位行；穿 3 件装备时卸下按钮不遮挡文案行；关闭钮可达；卖出/合并后弹窗自动收起（既有行为零回归）。
- H4 战斗 HUD：速度 x1/x2 切换、投降按钮文案与行为；开战按钮文案与行为。
- H5 出售区「出售」、备战引导中文一行居中（首帧 GlyphLayout 构建正常）。
- H6 三态横幅（胜利/战败/超时）+ 怜悯长行不越底板；终局面板四行中文 + 重新开始行为。
- H7 主菜单「开始远征」、装载屏「余烬军团/装载中……」（顺带验证 Fusion Pixel 加载）；可选：临时改名 assets/font 目录验证降级不炸后恢复（谨慎操作）。

---

## 8. 风险与开放问题（WARNING，不阻塞）

| # | 风险/问题 | 处置建议 |
|---|------|------|
| 1 | 任务描述称「12 条羁绊手写文案」，实测 synergies.json 仅 **6 条**（JsonLoaderTest.java:42 断言 6）；暗夜/精灵/植物/独眼与 Boss 为风味标签（data_schema §六 V1.3 刻意无羁绊） | 本计划按 6 条写全（CP4）；若用户预期是「扩羁绊至 12 条」，属玩法数值变更（新 thresholds/effects + 平衡），另立内容任务裁决 |
| 2 | 悬停卡最坏行数（双羁绊+长 desc ≈ 20~28 行）超过 15 行容量，必然截断（§5.3-4） | 已按裁决 1「只读精简」授权截断 + `…`；完整信息走点击详情弹窗。若手验发现档位行常态被截：优先调 `LINE_HEIGHT` 12→11 与卡高 +8（②③④ 空带余量内），或预览卡去掉羁绊 desc 行只留档位行——Phase 5.2 微调 |
| 3 | ShopCard enter/exit 在触屏 pointer≥0 也会触发（Scene2D 行为），Android 长按拖过卡片可能瞬时置 hoveredSlot | 悬停本就面向桌面；Android 无 mouseMoved 时棋盘源恒 -1，仅商店源可能瞬态（250ms 驻留 + 指针离开即清，实际难触发）；如需严格化可加 `pointer == -1` 守卫（一行），留手验后定 |
| 4 | CJK 降级：字体缺文件回退内置默认时全部中文不渲染（空白），不抛异常（§5.3-6） | 接受（裁决 3 全中文 + 不维护双语表）；数值仍可读、玩法不失效；正式构建字体已入库 |
| 5 | 锚点权衡两项（评审重点，均需用户确认接受）：(a) BOARD_HOVER 瞬态覆盖 ⑥ 开战按钮（指针移向按钮时卡已隐藏，无点击阻塞，论证见 §5.3-1；完全避让方案 76×162 会使行容量 15→12、列宽 7→6）；(b) SHOP_HOVER 覆盖 ⑤ 羁绊面板（悬停商店卡期间面板不可见，移开即恢复；右侧无其他可容纳 112 宽整块区域，⑥ 已挪左侧） | 两项均为瞬态视觉覆盖、零交互阻塞；(a) 备选 = 卡位 x148~224 × y128~290（避让 ⑥，容量/列宽下降）；(b) 备选 = 卡宽降至 76px 放 x 420~506 窄带（8 列 → 6 列，截断更严重）。默认按现方案，手验 H1/H2 不满意再换 |
| 6 | ResultBanner/RunEndPanel 中文绘字 x 为估算值（原英文定位不适用） | 手验 H6 微调（±8px 级），不构成结构风险 |
| 7 | 简报 R3「systems addNotice 英文串」实测已全中文（§3.4 #1），本计划未含 systems 文案改动 | 存档备查；若手验再发现英文动态行，按 §2.1 术语表补改（属 CP 级追加） |
| 8 | data_schema_design.md §六 字段表未回写 `desc`（差异声明 #1） | 建议 Phase 6 文档回写批次统一补（V1.5：desc 行 + skills desc 同款说明），本期不动设计文档（沿 Phase 5 惯例） |
| 9 | `UnitDetailDialog.refresh` 每帧重建行集 + `HoverPreviewCard.recompute` 行集缓存命中时零分配，但 detail 路径小量分配（沿 SynergyPanel WARNING-5 口径） | 名单 ≤27、60fps 可忽略；版本号缓存优化留后（与 SynergyPanel 同批） |
| 10 | CP9 弹窗增高至 252（y 44~296）后与 dialogStage 其他弹窗（宝箱 y 100~240、暂停 y 110~230）不同屏共存（弹窗栈互斥） | 无冲突；手验 H3 顺带确认 |

---

## 9. 附录：用户确认记录（2026-08-22 裁决原文存档，来源：简报 §1）

| # | 议题 | 裁决原文 |
|---|------|------|
| 1 | 悬停预览交互方案 | A 固定锚悬停卡：悬停 ~250ms 显示、不跟随鼠标、固定卡位（棋盘悬停与商店卡悬停各定锚点）、只读精简、拖拽中与模态期抑制、与详情弹窗共用格式化函数 |
| 2 | 羁绊描述来源 | B synergies.json 加 desc 字段（结构锁定变更已获批，手写文案） |
| 3 | 中文化范围 | A 全 UI 统一（术语按 GDD） |
| 4 | 执行链 | 简报 → planner 详细计划 → executor；#2 布局缺陷直修 |
